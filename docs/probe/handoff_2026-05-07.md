# PSUVPSC3DD handoff — 2026-05-07

This handoff started as the 2026-05-07 SCRREAM full-data VGGT/NOVA adapter handoff. It now also records the 2026-05-10 WAN Route2 setup, the 2026-05-11 SCRREAM meta-filter correction state, the 2026-05-12 WAN Route2 completion / Route2.1 implementation state, the 2026-05-15 Route2.1 / normalization / context / pred-x0 follow-up state, the 2026-05-16 pred-x0 / hidden-grid readout audit state, the 2026-05-17 CA-resampler layer-sweep state, the 2026-05-18 CA-resampler stability/full-grid and multi-source fusion state, the 2026-05-19 FLF2V / latent tensor-choice / hidden CA stability state, the 2026-05-19 VGGT-Omega / VGGT1 50ep baseline state, the 2026-05-19 22:50 CST final VGGT1 50ep result, and the 2026-05-20 Omega all-readout / NOVA probe-validity audit state.

## Update — 2026-05-20 14:45 CST

VGGT-Omega has now completed the intended immediate readout checks under the same clean SCRREAM / NOVA protocol:

- dense/full-token MLP, layers `12,16,20,24`;
- dense/full-token CA, layers `12,16,20,24`;
- register-only / "Frozen Scene Tokens" CA, layers `12,16,20,24`.

Best Omega result is dense CA layer `16`: `F@0.10=0.6576072630447046`, `pred_to_gt_p90=0.23843258867661157`, `Chamfer=0.033605430430422224`. This remains below both current VGGT1 references: MLP layer `16` (`F@0.10=0.702544731989172`) and CA layer `20` (`F@0.10=0.7014525243138799`). Register-only Omega CA is much weaker; its best run is layer `16` at `F@0.10=0.4930976482166729`.

Interpretation:

- do not claim from this alone that Omega has worse spatial representation;
- do not keep broadening Omega layer sweeps as the main line;
- the next main experiment is a decoder-free SCRREAM spatial readout. If Omega beats VGGT1 there but loses through NOVA/FM, this project line should be reframed as measuring compatibility with NOVA's condition-token manifold rather than pure spatial representation quality.

## Update — 2026-05-19 22:50 CST

VGGT1 50ep MLP/CA four-layer baselines completed and supersede the older 9510-step VGGT baseline as the current SCRREAM/VGGT reference.

Slurm state:

- `86571` / VGGT1 MLP-L4-H1024 layer pack completed `0:0` on `air-node-02`, elapsed `02:08:56`;
- `86572` / VGGT1 cross_attention-L4-H1024 layer pack completed `0:0` on `air-node-02`, elapsed `02:13:51`.

Final table:

| adapter | layer | F@0.10 | pred-to-GT p90 | Chamfer-L2 | final loss |
| --- | ---: | ---: | ---: | ---: | ---: |
| MLP | 12 | `0.5316993988168482` | `0.37930816908677417` | `0.07861695190270741` | `0.7722029089927673` |
| MLP | 16 | `0.702544731989172` | `0.19675995161135992` | `0.027118226668486994` | `0.7381577491760254` |
| MLP | 20 | `0.519013588803542` | `0.38937361538410187` | `0.07316385923574369` | `0.7465556263923645` |
| MLP | 24 | `0.6690686277634136` | `0.2389497272670269` | `0.0331160935262839` | `0.7303116321563721` |
| CA | 12 | `0.5065650562192365` | `0.33501065025726956` | `0.07443799295773108` | `0.677529513835907` |
| CA | 16 | `0.6835200371527321` | `0.23028291016817093` | `0.030130337458103895` | `0.6614104509353638` |
| CA | 20 | `0.7014525243138799` | `0.21181315431992212` | `0.027010128212471802` | `0.6642186045646667` |
| CA | 24 | `0.6204781376731087` | `0.2675088259081046` | `0.03806558856740594` | `0.6846963763237` |

Interpretation:

- Main baseline is now **VGGT1 MLP layer16 50ep**: `F@0.10=0.702544731989172`, `pred_to_gt_p90=0.19675995161135992`, `Chamfer=0.027118226668486994`.
- Strong readout comparison is **VGGT1 CA layer20 50ep**: `F@0.10=0.7014525243138799`, `pred_to_gt_p90=0.21181315431992212`, `Chamfer=0.027010128212471802`.
- CA does not globally dominate MLP, but CA strongly rescues layer20 (`MLP layer20 F@0.10=0.519013588803542` -> `CA layer20 F@0.10=0.7014525243138799`), so readout choice matters.
- Omega dense/full-token MLP, dense/full-token CA, and register-only CA all remain negative versus the new VGGT1 baseline. Best Omega is dense CA layer16 `F@0.10=0.6576072630447046`, while VGGT1 MLP layer16 is `0.702544731989172`.

Immediate next action:

1. Do not broaden Omega sweeps as the main line; dense MLP, dense CA, and register-only CA have already failed to match VGGT1.
2. Implement a decoder-free direct SCRREAM spatial readout to test VGGT1 vs Omega without the NOVA/FM generator.
3. Use VGGT1 MLP layer16 50ep and VGGT1 CA layer20 50ep as the two primary NOVA/FM comparison anchors.

## Update — 2026-05-19 19:04 CST

VGGT-Omega is wired and the first dense/full-token comparison completed, but it underperformed old clean-GT VGGT. This update records the setup state before the VGGT1 50-epoch baseline completion jobs finished; the final results are in the `2026-05-19 22:50 CST` update above.

Implementation state:

- `third_party/vggt-omega` is a Git submodule.
- `experiments/probe3d/train_vggt_nova_adapter.py` supports `--backbone vggt|vggt_omega`, `--vggt_omega_weights`, and `--vggt_omega_image_resolution`.
- VGGT-Omega loading goes through `experiments/probe3d/vggt_nova_adapter_common_raw.py`, expects official `checkpoints/vggt_omega/vggt_omega_1b_512.pt`, validates Omega-like state dict keys, and forces `aggregator.cached_layer_indices` to all layers.
- `checkpoints/vggt_omega/model.pt` was checked and is byte-identical to old `checkpoints/vggt/model.pt`; do not use it for Omega conclusions.
- Slurm launchers added:
  - `slurm/scrream_vggt_omega_download.sbatch`;
  - `slurm/scrream_vggt_omega_layer_ablation_pack_train.sbatch`.
- `slurm/scrream_vggt_layer_ablation_pack_train.sbatch` now supports `SCRREAM_ADAPTER_TYPE`, `SCRREAM_FEATURE_CACHE_RUN_PREFIX`, `SCRREAM_ADAPTER_HEADS`, and `SCRREAM_ADAPTER_MLP_RATIO`, so VGGT1 MLP/CA readout baselines can share old frozen VGGT feature caches while writing new output prefixes.

VGGT-Omega dense/full-token result:

| job | setting | F@0.10 | pred-to-GT p90 | Chamfer-L2 | final loss |
| ---: | --- | ---: | ---: | ---: | ---: |
| `86564` | Omega dense layer16 | `0.6550556579677611` | `0.23105039075016975` | `0.03261705581098795` | `0.7112300992012024` |
| `86564` | Omega dense layer24 | `0.6133813288610513` | `0.25702105338374776` | `0.03972778360669812` | `0.733043909072876` |

Comparison anchors:

- old clean-GT VGGT layer16: `F@0.10=0.6860468604251301`, `pred_to_gt_p90=0.20770130679011345`, `Chamfer=0.026904070439438026`;
- old clean-GT VGGT layer24: `F@0.10=0.6802513448244976`, `pred_to_gt_p90=0.25152526050806046`, `Chamfer=0.03554012098660072`.

Interpretation:

- Omega dense/full-token integration is live, but the first layer16/24 result is below old VGGT.
- Treat this as a red flag to audit feature branch, layer mapping, preprocessing, and readout choice after VGGT1 50ep baselines complete.
- Do not yet claim Omega is scientifically worse than old VGGT.
- Superseded by the `2026-05-20 14:45 CST` update above: paper-faithful register-only "Frozen Scene Tokens" CA later completed and did not rescue Omega.

VGGT1 50ep baseline state at this earlier checkpoint, superseded by the 22:50 final table above:

- CA smoke job `86568` completed `0:0`.
- Accidental seven-layer jobs `86569` / `86570` were cancelled.
- Four-layer jobs were running on `air-node-02` at 19:04 and later completed successfully:
  - `86571`: MLP-L4-H1024, layers `12,16,20,24`;
  - `86572`: cross_attention-L4-H1024, layers `12,16,20,24`.
- Both use 50 epochs / `15850` max steps, `eval_batches=0`, `val_preview_samples=12`, and `val_preview_queries=40960`.
- Outputs:
  - `experiments/probe3d/result/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_vggt1_mlp_l4_h1024_nova_flow_robustval_metafilter_seed17_50ep_layerXX`;
  - `experiments/probe3d/result/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_vggt1_ca_l4_h1024_nova_flow_robustval_metafilter_seed17_50ep_layerXX`.
- At 19:04, validation files for layer12/16 were intermediate only and layer20/24 had not written final metrics yet; this is no longer the current state.

Supersession note:

- The wait and final-table build were completed in the 22:50 update above.
- Current next action has moved past Omega readout audit: dense CA and register-only CA are complete and negative versus VGGT1, so the next action is the decoder-free probe-validity audit described in the 2026-05-20 update.

## Update — 2026-05-19 14:14 CST

WAN is paused as a main branch after the Route2 / Route2.1 / readout audit.

- Final summary: `docs/probe/wan_summary_2026-05-19.md`.
- No WAN Slurm jobs are active at this checkpoint.
- Keep WAN code, caches, and results: they remain useful as a reference implementation for feature-cache probing, CA-resampler readout, FLF2V routing, and optional appendix checks.
- Do not launch more broad WAN sweeps before testing the next backbone.

Frozen conclusion:

- WAN T2V hidden states contain usable spatial signal, but the learned `wan_cross_attn_resampler` is required to expose it well under the NOVA/FM decoder.
- Historical best WAN is CA `t499/layer14`: `F@0.10=0.5012956284974035`, `pred_to_gt_p90=0.4229188362757365`, `Chamfer=0.10908368105689685`.
- More stable repeated WAN settings are CA `t249/layer14` by F-score (`0.48913902331806663`) and CA `t249/layer09` by p90 / Chamfer (`0.410525918006897` / `0.11981592203179996`).
- L4 CA `t249/layer14` is a small positive (`F@0.10=0.4919946248489035`) but not a new peak.
- Noise/no-noise, token norm, pair_tiled81, pred-x0/model-output latent, fixed grid readouts, multi-layer fusion, multi-timestep fusion, FLF2V hidden, and gated CA did not close the gap.
- At this WAN pause checkpoint, the then-current 9510-step clean-GT VGGT layer `16` reference was much stronger (`F@0.10=0.6860468604251301`). The 22:50 update above supersedes this comparison anchor with VGGT1 MLP layer16 50ep.

Next model protocol:

1. keep the same clean SCRREAM `.pt`, split, mesh-complete GT, NOVA decoder, loss, and robust validation;
2. start with cache shape / metadata checks and a zero or sample-shuffle control;
3. run one-step adapter smoke before full training;
4. test only a small layer/readout pilot first;
5. if fixed pooling is weak, move quickly to a learned cross-attention resampler instead of launching a broad layer sweep.

## Update — 2026-05-19 13:13 CST

WAN hidden CA stability/capacity jobs completed:

| job | setting | F@0.10 | pred-to-GT p90 | Chamfer-L2 | interpretation |
| ---: | --- | ---: | ---: | ---: | --- |
| `86547` | standard hidden CA seed23, `t249/layer14` | `0.48706357506233194` | `0.4482837840914726` | `0.11202508273224036` | close to full-grid `t249/layer14`, confirms variance |
| `86548` | standard hidden CA seed23, `t499/layer14` | `0.4591392560862819` | `0.4479780395825704` | `0.14107579924166203` | below historical `t499/layer14` peak |
| `86549` | gated hidden CA seed17, `t249/layer14` | `0.4505522726705196` | `0.46173084527254105` | `0.11595033543805282` | gated residual CA negative by F-score |
| `86550` | gated hidden CA seed17, `t499/layer14` | `0.4866296484297818` | `0.45513606319824856` | `0.12565729891260466` | decent but below historical peak |
| `86553` | standard hidden CA L4/H512 seed17, `t249/layer14` | `0.4919946248489035` | `0.4724609777331352` | `0.11948200377325217` | best recent repeated-F result, small positive |
| `86554` | standard hidden CA L4/H512 seed17, `t499/layer14` | `0.46280321302050603` | `0.44610939423243207` | `0.13269949393967786` | negative versus historical `t499/layer14` |

Comparison anchors:

- historical WAN peak remains CA `t499/layer14` from job `86429`: `F@0.10=0.5012956284974035`, `pred_to_gt_p90=0.4229188362757365`, `Chamfer=0.10908368105689685`;
- strongest repeated full-grid F-score remains CA `t249/layer14` from job `86468`: `F@0.10=0.48913902331806663`, `pred_to_gt_p90=0.4372795696059863`, `Chamfer=0.12182091859479745`;
- strongest repeated p90 / Chamfer remains CA `t249/layer09` from job `86468`: `pred_to_gt_p90=0.410525918006897`, `Chamfer=0.11981592203179996`, with `F@0.10=0.4766100401399189`;
- At this checkpoint, the then-current 9510-step clean-GT VGGT layer `16` reference remained much stronger: `F@0.10=0.6860468604251301`, `pred_to_gt_p90=0.20770130679011345`, `Chamfer=0.026904070439438026`. The 22:50 update above supersedes the anchor with VGGT1 MLP layer16 50ep.

Interpretation:

- hidden CA remains the best WAN path under the NOVA/FM decoder, but training variance is large;
- L4 hidden CA at `t249/layer14` is a small positive over the repeated full-grid F-score, but it worsens p90 relative to `t249/layer09` and does not beat the historical peak;
- gated residual CA did not improve enough to expand;
- latent tensor-choice, simple hidden fusion, FLF2V hidden, and simple grid readouts remain stopped branches.

Optional WAN appendix branch if WAN is explicitly reopened:

1. inspect visuals for full-grid `t249/layer09`, full-grid `t249/layer14`, and L4 `t249/layer14`;
2. run only narrow L4 hidden-CA checks, especially `t249/layer09 + adapter_layers=4` and one seed repeat for L4 `t249/layer14`;
3. do not spend more broad sweeps on gated CA, latent tensors, simple fusion, FLF2V hidden, or layer24/29 single-layer probes without new visual evidence.

Queue state at this check: no WAN jobs were active; only unrelated Slurm jobs were present.

## Update — 2026-05-19 10:51 CST

WAN latent tensor-choice audit completed:

- `model_output_latent` cache job `86534` completed `0:0`, elapsed `01:00:50`, with `329/329` files under:
  - `experiments/probe3d/feature_cache/scrream_wan_t2v1p3b_ctx81_model_output_latent/t499/model_output_latent`
  - cache tensor shape `[12480,16]`, metadata `model_output_formula=transformer_output_sample`;
- smoke job `86535` completed `0:0`;
- formal `model_output_latent + conv2d_mlp` job `86536` completed `0:0`, output:
  - `experiments/probe3d/result/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_mlp_l4_nova_flow_robustval_metafilter_seed17_wan_t2v1p3b_ctx81_model_output_latent_conv2d_t499`
  - metrics: `best_val_fscore_tau_0.10=0.4199299775478907`, `best_val_pred_to_gt_p90=0.6285491685072581`, `best_val_chamfer_l2=0.44791099180777866`;
- learned latent CA readout was implemented as `WanLatentCrossAttentionResamplerAdapter` / `--adapter_type wan_latent_cross_attn_resampler`. It restores latent caches as `[B,2,60,104,16]`, adds temporal/row/column position embeddings, and cross-attends `768` NOVA query tokens to the latent grid;
- `model_output_latent + wan_latent_cross_attn_resampler` smoke `86539` and formal `86540` completed `0:0`; formal output:
  - `experiments/probe3d/result/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_mlp_l4_nova_flow_robustval_metafilter_seed17_wan_t2v1p3b_ctx81_model_output_latent_ca_resampler_t499`
  - metrics: `best_val_fscore_tau_0.10=0.4289946537162989`, `best_val_pred_to_gt_p90=0.5942087918519974`, `best_val_chamfer_l2=0.19963246708114943`;
- high-memory pred-x0 learned-CA smoke `86541` was cancelled and replaced by lower-memory smoke `86542`, which completed `0:0`;
- formal `pred_x0_latent + wan_latent_cross_attn_resampler` job `86543` completed `0:0`, output:
  - `experiments/probe3d/result/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_mlp_l4_nova_flow_robustval_metafilter_seed17_wan_t2v1p3b_ctx81_pred_x0_latent_ca_resampler_t499`
  - metrics: `best_val_fscore_tau_0.10=0.43766060222664477`, `best_val_pred_to_gt_p90=0.5175856028993925`, `best_val_chamfer_l2=0.20880577837427458`.

Interpretation:

- learned latent CA improves over fixed Conv2d for some metrics, but all latent tensor-choice results remain below the hidden CA baselines;
- stop latent tensor-choice expansion for now unless the user explicitly asks;
- the strongest WAN path remains hidden-state `wan_cross_attn_resampler`.

WAN-only stability/capacity branch submitted at this timestamp, later completed in the `2026-05-19 13:13 CST` update above:

- `experiments/probe3d/train_wan_t2v_nova_adapter.py` now exposes `--adapter_gated`, which enables zero-initialized residual gates in WAN CA blocks;
- `slurm/scrream_wan_t2v_ablation_pack_train.sbatch` now exposes `WAN_SEED` and `WAN_ADAPTER_GATED`;
- checks passed:
  - `conda run -n nova3r python -m py_compile experiments/probe3d/probe/adapter.py experiments/probe3d/train_wan_t2v_nova_adapter.py experiments/probe3d/scripts/prepare_scrream_wan_t2v_feature_cache.py`
  - `bash -n slurm/scrream_wan_t2v_ablation_pack_train.sbatch slurm/scrream_wan_t2v_precompute.sbatch slurm/scrream_wan_t2v_multilayer_pack_train.sbatch slurm/scrream_wan_t2v_multitime_pack_train.sbatch slurm/scrream_wan_flf2v_train.sbatch`
  - local dummy hidden CA gated/ungated forward-backward `[1,3120,1536] -> [1,768,128]`;
- Slurm jobs submitted and running on `air-node-02` at this timestamp:
  - `86547`: standard hidden CA seed23, `t249/layer14`;
  - `86548`: standard hidden CA seed23, `t499/layer14`;
  - `86549`: gated hidden CA seed17, `t249/layer14`;
  - `86550`: gated hidden CA seed17, `t499/layer14`.
- additional hidden CA capacity jobs submitted shortly after:
  - `86553`: standard hidden CA with `adapter_layers=4`, `hidden_dim=512`, seed17, `t249/layer14`;
  - `86554`: standard hidden CA with `adapter_layers=4`, `hidden_dim=512`, seed17, `t499/layer14`.

Original interpretation gate for `86547`-`86554`, superseded by the completed results above:

- compare `t249/layer14` against the full-grid repeat baseline `F@0.10=0.48913902331806663`, `p90=0.4372795696059863`, `Chamfer=0.12182091859479745`;
- compare `t499/layer14` against the historical peak `F@0.10=0.5012956284974035`, `p90=0.4229188362757365`, `Chamfer=0.10908368105689685`, while remembering the exact full-grid repeat dropped to `F@0.10=0.429506998327231`;
- if gated CA clearly improves repeatability or beats the baselines, expand that variant; otherwise treat hidden CA as the current WAN ceiling under this NOVA/FM decoder setup.

## Update — 2026-05-19 07:45 CST

WAN hidden-fusion and FLF2V follow-up state:

- T2V multi-timestep same-layer fusion job `86494` completed `0:0`, elapsed `01:16:22`, output:
  - `experiments/probe3d/result/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_mlp_l4_nova_flow_robustval_metafilter_seed17_wan_t2v1p3b_ctx81_ca_resampler_multitime_timesteps249-499-749_layer14`
  - metrics: `best_val_fscore_tau_0.10=0.45213117002164466`, `best_val_pred_to_gt_p90=0.49119475732247037`, `best_val_chamfer_l2=0.12659800921877226`;
  - interpretation: same-layer multi-timestep hidden fusion is weaker than single-layer CA full-grid `t249/layer14` and `t249/layer09`, so stop simple hidden-fusion variants for now.
- FLF2V checkpoint retry/preflight job `86500` completed after the initial `86497` incomplete-read failure. Full FLF2V 480P cache completed through shard jobs `86515` / `86516` / `86517` / `86518` with `329/329` files under:
  - `experiments/probe3d/feature_cache/scrream_wan_flf2v14b_pair_endpoint81_480/t249/layer14`
  - cache tensors are `[3120,5120]`, dtype `float16`, with `feature_source=wan_flf2v_hidden`, `window_mode=pair_endpoint81`, `condition_frames=first_last`, `temporal_indices=[0,20]`, `grid_shape=[21,30,52]`, `timestep_index=249`, `scheduler_timestep=979.0`, `layer=14`, `height=480`, and `width=832`.
- FLF2V smoke train job `86519` completed `0:0`; formal train job `86520` completed `0:0`, elapsed `01:34:41`, output:
  - `experiments/probe3d/result/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_mlp_l4_nova_flow_robustval_metafilter_seed17_wan_flf2v14b_pair_endpoint81_t249_layer14`
  - metrics: `best_val_fscore_tau_0.10=0.42897984457682026`, `best_val_pred_to_gt_p90=0.5051772321263949`, `best_val_chamfer_l2=0.14052311765650907`;
  - interpretation: FLF2V hidden is live but weaker than T2V CA baselines (`t249/layer14=0.48913902331806663`, historical `t499/layer14=0.5012956284974035`), so do not expand FLF2V hidden broadly yet.

WAN tensor-choice audit started at this timestamp, later completed in the `2026-05-19 10:51 CST` update above:

- `experiments/probe3d/scripts/prepare_scrream_wan_t2v_feature_cache.py` now supports `--feature_kind model_output_latent`, which caches raw WAN transformer `output.sample` latent slices. Cache layout is `t499/model_output_latent/<sample>.pt`, expected pair shape `[12480,16]`, metadata includes `model_output_latent_shape` and `model_output_formula=transformer_output_sample`.
- `experiments/probe3d/train_wan_t2v_nova_adapter.py` supports `--wan_feature_kind model_output_latent`. `--adapter_type conv2d_mlp` is valid for both `pred_x0_latent` and `model_output_latent`, restoring `[B,2,60,104,16]` before the Conv2d latent readout.
- `slurm/scrream_wan_t2v_precompute.sbatch` and `slurm/scrream_wan_t2v_ablation_pack_train.sbatch` route `WAN_FEATURE_KIND=model_output_latent` to cache root:
  - `experiments/probe3d/feature_cache/scrream_wan_t2v1p3b_ctx81_model_output_latent`
- Static checks passed:
  - `python3 -m py_compile experiments/probe3d/scripts/prepare_scrream_wan_t2v_feature_cache.py experiments/probe3d/train_wan_t2v_nova_adapter.py experiments/probe3d/probe/adapter.py`
  - `bash -n slurm/scrream_wan_t2v_precompute.sbatch`
  - `bash -n slurm/scrream_wan_t2v_ablation_pack_train.sbatch`
- Slurm chain submitted at this timestamp:
  - `86534`: full `model_output_latent` cache, `WAN_TIMESTEPS=499`, `WAN_FEATURE_KIND=model_output_latent`;
  - `86535`: one-step `conv2d_mlp` training smoke, dependency `afterok:86534`;
  - `86536`: 50-epoch formal train, dependency `afterok:86535`;
  - all three later completed and are recorded in the `2026-05-19 10:51 CST` update above.

Interpretation gate for `86536`: compare against old hidden MLP `t499/layer09` (`F@0.10=0.46988987902779306`), repeated T2V CA `t249/layer14` (`0.48913902331806663`), historical T2V CA `t499/layer14` (`0.5012956284974035`), and FLF2V hidden (`0.42897984457682026`).

## Update — 2026-05-18 13:37 CST

WAN multi-source hidden fusion implementation and execution state:

- `experiments/probe3d/probe/adapter.py` now has a shared `WanHiddenMultiSourceCrossAttentionResamplerAdapter` for multiple same-shaped WAN hidden sources. It restores concatenated tokens as `[source, temporal, row, col]`, adds learnable source / temporal / row / column position embeddings, and cross-attends `768` learned NOVA query tokens to the WAN hidden tokens.
- `train_wan_t2v_nova_adapter.py` now supports:
  - `--wan_layers "9,14"` with `--adapter_type wan_cross_attn_multilayer_resampler`;
  - `--wan_timesteps "249,499,749"` with `--adapter_type wan_cross_attn_multitime_resampler`;
  - hidden-cache-only validation for both modes;
  - explicit errors if multi-layer and multi-timestep lists are both multi-valued;
  - config / `final_metrics.json` fields for `wan_layers`, `num_wan_layers`, `wan_timesteps`, `num_wan_timesteps`, and source metadata.
- New Slurm launchers:
  - `slurm/scrream_wan_t2v_multilayer_pack_train.sbatch`;
  - `slurm/scrream_wan_t2v_multitime_pack_train.sbatch`.
- `train_wan_t2v_nova_adapter.py` wraps `swanlab.finish()` in `try/except` and logs `SwanLab finish warning` so a late upload/proxy teardown failure does not invalidate already-written metrics/checkpoints.

Multi-layer execution:

- smoke job `86483` completed `0:0` for `t249 layers 9+14`;
- bad submit job `86484` was cancelled because comma-containing `--export` only launched one group;
- formal job `86485` completed `0:0`, elapsed `01:02:13`, output:
  - `experiments/probe3d/result/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_mlp_l4_nova_flow_robustval_metafilter_seed17_wan_t2v1p3b_ctx81_ca_resampler_multilayer_t249_layers9-14`
  - metrics: `best_val_fscore_tau_0.10=0.46487238144059545`, `best_val_pred_to_gt_p90=0.49916083614031476`, `best_val_chamfer_l2=0.12842474008599916`;
- high-QOS job `86486` was cancelled because of the one-high-QOS-job-per-user limit;
- formal job `86487` completed training/writeout but Slurm reported `FAILED 1:0` because `swanlab.finish()` hit a proxy error after metrics/checkpoints were written; output:
  - `experiments/probe3d/result/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_mlp_l4_nova_flow_robustval_metafilter_seed17_wan_t2v1p3b_ctx81_ca_resampler_multilayer_t249_layers9-14-19`
  - metrics: `best_val_fscore_tau_0.10=0.4724058254433277`, `best_val_pred_to_gt_p90=0.449202927450339`, `best_val_chamfer_l2=0.12805132629970709`.

Interpretation:

- multi-layer fusion did not beat single-layer `t249/layer14` (`F@0.10=0.48913902331806663`) or single-layer `t249/layer09` p90 / Chamfer (`0.410525918006897` / `0.11981592203179996`);
- stop simple same-timestep hidden-layer fusion for now.

Multi-timestep state at this timestamp, superseded by the 2026-05-19 update above:

- smoke job `86493` completed `0:0`, elapsed `00:01:20`, for timesteps `249+499+749`, layer `14`, with selected feature shape `[1,9360,1536]`;
- formal job `86494` was running on `air-node-02` at this timestamp, output:
  - `experiments/probe3d/result/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_mlp_l4_nova_flow_robustval_metafilter_seed17_wan_t2v1p3b_ctx81_ca_resampler_multitime_timesteps249-499-749_layer14`
  - log: `slurm_out/scrream_wan_t2v_multitime_timesteps249-499-749_layer14_86494.log`
  - final metrics were later recorded in the 2026-05-19 update above.

Recommended next branch:

- inspect `86494` after it exits;
- if `86494` is negative, stop hidden-fusion variants and move to final WAN transformer output / noise-pred-like tensor with the learned resampler interface;
- keep I2V/FLF2V condition-side features as a later, higher-cost branch.

## Update — 2026-05-18 10:06 CST

CA-resampler stability and full-grid audit completed:

- old pending low-LR job `86444` was cancelled because its `128G` memory request blocked scheduling; replacement low-LR job `86453` used `48G`, ran on `air-node-02`, completed `0:0`, elapsed `01:10:59`;
- `86453` result for `t499/layer14 + wan_cross_attn_resampler`, `WAN_LR=5e-5`: `best_val_fscore_tau_0.10=0.4292316005720653`, `best_val_pred_to_gt_p90=0.5338096991181374`, `best_val_chamfer_l2=0.15617707930505276`; this is a negative result versus both old hidden and the original CA layer14 run;
- full-grid job `86468` ran `WAN_LR=1e-4`, `WAN_PACK_PARALLEL=3`, `timesteps=249,499,749`, `layers=9,14,19,24,29` on `air-node-02`, completed `0:0`, elapsed `06:31:56`;
- full-grid best F@0.10 is `t249/layer14`: `0.48913902331806663`, `pred_to_gt_p90=0.4372795696059863`, `Chamfer=0.12182091859479745`;
- full-grid best p90 / Chamfer is `t249/layer09`: `F@0.10=0.4766100401399189`, `pred_to_gt_p90=0.410525918006897`, `Chamfer=0.11981592203179996`;
- historical best WAN remains the original `86429` `t499/layer14` peak: `F@0.10=0.5012956284974035`, `pred_to_gt_p90=0.4229188362757365`, `Chamfer=0.10908368105689685`, but the exact full-grid repeat of `t499/layer14` dropped to `F@0.10=0.429506998327231`;
- interpretation: CA-resampler is still the only clearly positive WAN readout family, but training variance is large. Treat `t499/layer14=0.5013` as historical peak, and treat `t249/layer09/14` as the strongest repeated settings until repeat runs clarify stability.

Recommended next branch:

- inspect `val_visual_40960` for full-grid `t249/layer09` and `t249/layer14`;
- run repeat checks for `t249/layer09`, `t249/layer14`, and historical `t499/layer14`;
- avoid further single-layer `layer24/29` WAN probes unless doing multi-layer fusion;
- keep final WAN transformer output / noise-pred-like tensor and I2V/FLF2V condition-side features as later tensor-choice branches.

## Update — 2026-05-16 22:26 CST

WAN readout audits completed:

- pred-x0 Conv2d formal job `86422` completed on `air-node-02`, exit `0:0`, elapsed `00:52:13`, output:
  - `experiments/probe3d/result/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_mlp_l4_nova_flow_robustval_metafilter_seed17_wan_t2v1p3b_ctx81_pred_x0_latent_conv2d_t499`
- pred-x0 Conv2d metrics: `best_val_fscore_tau_0.10=0.41336424426176693`, `best_val_pred_to_gt_p90=0.6272750149170557`, `best_val_chamfer_l2=0.35522504647572833`;
- hidden `grid2d_pool` smoke/formal jobs `86423` / `86424` completed on `air-node-04`; formal metrics: `best_val_fscore_tau_0.10=0.43326753863230877`, `best_val_pred_to_gt_p90=0.5651116619507471`, `best_val_chamfer_l2=0.1759416777640581`;
- hidden `grid2d_conv` smoke/formal jobs `86425` / `86426` completed on `air-node-04`; formal metrics: `best_val_fscore_tau_0.10=0.39855373112050535`, `best_val_pred_to_gt_p90=0.7689011543989182`, `best_val_chamfer_l2=1.159957120815913`.

Interpretation:

- old WAN hidden `ctx81 normal t499/layer09` remained the best WAN run at this timestamp: `F@0.10=0.46988987902779306`, `pred_to_gt_p90=0.48718947172164917`, `Chamfer-L2=0.21040735269586244`; this was superseded by the `2026-05-17 02:20 CST` CA-resampler layer14 result below;
- At this checkpoint, the then-current 9510-step clean-GT VGGT layer `16` reference remained much stronger: `F@0.10=0.6860468604251301`, `pred_to_gt_p90=0.20770130679011345`, `Chamfer-L2=0.026904070439438026`;
- noise level, token normalization, pair-vs-context framing, pred-x0 latent choice, and simple fixed grid pooling/conv do not explain the WAN gap;
- the WAN branch recommended at this timestamp was to keep the NOVA/FM generator and try a learned Perceiver / cross-attention resampler from the best WAN hidden tokens (`ctx81 normal t499/layer09`) to NOVA condition tokens; this branch is now implemented and recorded in the `2026-05-17` updates below;
- keep final WAN transformer output / noise-pred-like tensor as a later tensor-choice audit rather than the immediate branch.

Execution note added later on `2026-05-16 22:50 CST`:

- `WanHiddenCrossAttentionResamplerAdapter` / `--adapter_type wan_cross_attn_resampler` has been implemented for hidden WAN cache only;
- it restores the expected `[B,3120,1536]` hidden cache as a `[2,30,52]` temporal/spatial grid with learnable temporal/row/column position embeddings, then uses `768` learnable query tokens to cross-attend to all WAN tokens and outputs NOVA condition tokens `[B,768,128]`;
- local `nova3r` dummy forward/backward passed for `hidden_dim=512`, `adapter_layers=2`, `heads=8`;
- Slurm smoke job `86427` was submitted and started on `air-node-02`;
- Slurm formal job `86428` was submitted with `afterok:86427`, same setting, 50 epochs / full robust validation.

Update added on `2026-05-17 00:05 CST`:

- `86427` completed `0:0`; `86428` completed `0:0` on `air-node-02`, elapsed `00:54:48`;
- `86428` result for `ctx81 normal t499/layer09 + wan_cross_attn_resampler`: `best_val_fscore_tau_0.10=0.4675106769755634`, `best_val_pred_to_gt_p90=0.4791356101632118`, `best_val_chamfer_l2=0.1388903713474671`;
- this nearly matches old WAN hidden `t499/layer09` on F@0.10 (`0.46988987902779306`) while improving p90 and Chamfer, so learned token readout remains the best WAN interface direction so far;
- layer sweep job `86429` was submitted with `WAN_LAYERS=14,19,24,29`, `WAN_PACK_PARALLEL=3`, same adapter settings (`layers=2`, `hidden_dim=512`, `heads=8`) and is running on `air-node-02`; layer09 is already covered by `86428`.

Update added on `2026-05-17 02:20 CST`:

- layer sweep job `86429` completed `0:0` on `air-node-02`, elapsed `02:14:39`;
- CA resampler `t499/layer14` became the historical WAN peak: `best_val_fscore_tau_0.10=0.5012956284974035`, `best_val_pred_to_gt_p90=0.4229188362757365`, `best_val_chamfer_l2=0.10908368105689685`;
- compared with old WAN hidden `t499/layer09`, this is `+0.03140575` F@0.10 (`+6.68%` relative), `-0.06427064` pred-to-GT p90 (`-13.19%` relative), and `-0.10132367` Chamfer-L2 (`-48.16%` relative);
- full layer sweep results: layer09 `0.46751068`, layer14 `0.50129563`, layer19 `0.45041048`, layer24 `0.40883680`, layer29 `0.38334162` on F@0.10;
- `best_fscore_010.pth`, `best_pred_to_gt_p90.pth`, and `best.pth` for layer14 all point to the same metric tuple; visual previews are available under `val_visual_40960/step_011500/`;
- recommended branch at this timestamp: inspect layer14 visual previews, then run a low-LR stability rerun for `t499/layer14` before expanding timesteps. This was superseded by the `2026-05-18 10:06 CST` update above.

Update added on `2026-05-17 14:26 CST`:

- user visually inspected CA resampler layer14 and reported the visual result improved;
- submitted low-LR stability rerun job `86444` for `t499/layer14`, `WAN_ADAPTER_TYPE=wan_cross_attn_resampler`, `WAN_LR=5e-5`, `WAN_VAL_EVERY=250`, `adapter_layers=2`, `hidden_dim=512`, `heads=8`;
- output prefix is `scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_mlp_l4_nova_flow_robustval_metafilter_seed17_wan_t2v1p3b_ctx81_ca_resampler_lowlr5e5_`, so it will not overwrite the layer14 sweep result;
- at submission time job `86444` had not yet started; it was later cancelled and replaced by job `86453`, as recorded in the `2026-05-18 10:06 CST` update above.

## Update — 2026-05-16 14:05 CST

Predicted-x0 / denoised-latent follow-up:

- full pred-x0 cache job `86411` completed `329/329` samples under `experiments/probe3d/feature_cache/scrream_wan_t2v1p3b_ctx81_pred_x0_latent/t499/pred_x0_latent/`;
- cache feature shape is `[12480,16]`; metadata records `sigma`, `x0_formula`, `pred_x0_latent_shape`, and latent/model-output shapes;
- original pred-x0 MLP readout did not produce a valid formal result: `86412` failed on the first backward with a PyTorch CUDA `AdaptiveAveragePooling` shared-memory assert from pooling `[12480,128] -> [768,128]`;
- `experiments/probe3d/probe/adapter.py` now includes `WanPredX0LatentConvAdapter`;
- `experiments/probe3d/train_wan_t2v_nova_adapter.py` now supports `--adapter_type conv2d_mlp`, restricted to `--wan_feature_kind pred_x0_latent`;
- `slurm/scrream_wan_t2v_ablation_pack_train.sbatch` now passes `WAN_ADAPTER_TYPE`, with a pred-x0 conv output prefix ending in `wan_t2v1p3b_ctx81_pred_x0_latent_conv2d_`;
- Conv2d readout restores `[B,2,60,104,16]`, applies a small stride-2 Conv2d stem to `[B,3120,128]`, then pools to NOVA scene tokens `[B,768,128]`;
- local shape/backward smoke passed, and Slurm smoke job `86421` completed on `air-node-02`, exit `0:0`, with robust validation and no pooling crash;
- formal pred-x0 conv job `86422` had been submitted on `air-node-02`, `1 x A100`, `QOS=high`, output:
  - `experiments/probe3d/result/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_mlp_l4_nova_flow_robustval_metafilter_seed17_wan_t2v1p3b_ctx81_pred_x0_latent_conv2d_t499`

This was later completed and superseded by the `2026-05-16 22:26 CST` update above.

## Update — 2026-05-15 23:10 CST

WAN normalization and context audits completed:

- `t499/layer09 + token_layernorm` completed as job `86395`: `best_val_fscore_tau_0.10=0.45476213575933216`, `best_val_pred_to_gt_p90=0.484821617603302`, `best_val_chamfer_l2=0.1771892917652925`;
- `pair_tiled81` versus `ctx81` completed as jobs `86396 -> 86397 -> 86400 -> 86401`: `best_val_fscore_tau_0.10=0.4429200036613287`, `best_val_pred_to_gt_p90=0.5104739194115003`, `best_val_chamfer_l2=0.16478415516515574`;
- neither setting beat the old WAN Route2 best `t499/layer09` (`F@0.10=0.46988987902779306`, `pred_to_gt_p90=0.48718947172164917`).

Predicted-x0 / denoised-latent feature probe was implemented:

- `experiments/probe3d/scripts/prepare_scrream_wan_t2v_feature_cache.py --feature_kind pred_x0_latent` computes WAN scheduler clean-latent estimates with `x0 = sample - sigma * model_output`;
- pred-x0 cache layout is `experiments/probe3d/feature_cache/scrream_wan_t2v1p3b_ctx81_pred_x0_latent/t499/pred_x0_latent/<sample>.pt`;
- expected feature shape is `[12480,16]` from two temporal slices of the `[21,60,104,16]` latent grid;
- metadata records `feature_kind`, `sigma`, scheduler class / prediction type / `predict_x0`, source latent shape, transformer latent shape, model output shape, pred-x0 latent shape, and `x0_formula=sample_minus_sigma_model_output`;
- `experiments/probe3d/train_wan_t2v_nova_adapter.py --wan_feature_kind pred_x0_latent` reads this cache; this original MLP-L4 readout was later superseded by the 2026-05-16 Conv2d readout after the MLP pooling path failed;
- `slurm/scrream_wan_t2v_precompute.sbatch` and `slurm/scrream_wan_t2v_ablation_pack_train.sbatch` expose `WAN_FEATURE_KIND=pred_x0_latent`; non-hidden feature kinds ignore block-layer sweeps and run once per timestep.

Queued Slurm chain at this timestamp, later superseded by the 2026-05-16 `86411` / `86421` / `86422` path:

- `86406`: 2-sample pred-x0 cache smoke, pending on resources;
- `86409`: 1-step pred-x0 train smoke, depends on `86406`;
- `86408`: full 329-sample pred-x0 cache, depends on `86409`;
- `86410`: formal 50-epoch pred-x0 training, depends on `86408`;
- all four jobs request `1 x A100`.

This queue was later completed or superseded by the 2026-05-16 pred-x0 Conv2d path and the 2026-05-16 hidden-grid readout audits above.

## Update — 2026-05-15 12:30 CST

WAN Route2.1 clean / low-noise audit completed:

- cache smokes `86342` (`low_noise`) and `86343` (`no_noise`) completed successfully;
- full caches `86350` / `86351` completed successfully;
- no-noise smoke train `86357` completed successfully;
- replacement 1-GPU formal packs `86371` (`no_noise`) and `86372` (`low_noise`) completed on `air-node-04`, both exit `0:0`, about `02:29` elapsed each;
- no-noise cache root `experiments/probe3d/feature_cache/scrream_wan_t2v1p3b_ctx81_no_noise` has `329` samples for each of `t999/layer09`, `t999/layer14`, and `t999/layer29`;
- low-noise cache root `experiments/probe3d/feature_cache/scrream_wan_t2v1p3b_ctx81_low_noise` has the same complete coverage.

Route2.1 result:

| mode | layer | best F@0.10 | best pred->GT p90 | best Chamfer-L2 |
| --- | ---: | ---: | ---: | ---: |
| no_noise | 29 | 0.44840173 | 0.48779308 | 0.23386016 |
| low_noise | 09 | 0.44481815 | 0.57811778 | 0.20908525 |
| no_noise | 14 | 0.43563730 | 0.54730518 | 0.16860280 |

Interpretation: clean / low-noise WAN hidden states did not beat old Route2 best `t499/layer09` (`F@0.10=0.46988987902779306`, `pred_to_gt_p90=0.48718947172164917`). The weakness is unlikely to be explained only by latent noise level.

Targeted normalization follow-up, later completed in the 2026-05-16 update above:

- `experiments/probe3d/train_wan_t2v_nova_adapter.py` now supports `--wan_feature_norm none|token_layernorm|sample_standardize|token_l2`;
- `slurm/scrream_wan_t2v_ablation_pack_train.sbatch` passes the option through `WAN_FEATURE_NORM`;
- job `86395` runs `t499/layer09 + token_layernorm`, output:
  - `experiments/probe3d/result/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_mlp_l4_nova_flow_robustval_metafilter_seed17_wan_t2v1p3b_ctx81_norm_token_layernorm_t499_layer09`

Adapter-capacity note: the current MLP adapter already uses `GELU`. If normalization is inconclusive, low-priority capacity/readout checks can test `MLP-L6-H1024`, `MLP-L4-H2048`, or cross-attention; this is probably not the core cause of WAN underperformance.

## Update — 2026-05-12

Execution-state refresh at `2026-05-12 16:32 CST`:

- Slurm job `86307` / `scrream_wan_t2v_pack` completed on `air-node-04`, exit `0:0`, elapsed `13:36:04`;
- all 15 formal WAN Route2 runs now have `final_metrics.json`;
- best formal WAN is `t499/layer09` with `best_val_fscore_tau_0.10=0.46988987902779306`, `best_val_pred_to_gt_p90=0.48718947172164917`, and `best_val_chamfer_l2=0.21040735269586244`;
- At this checkpoint, the then-current 9510-step clean-GT VGGT layer `16` reference remained much stronger with `best_val_fscore_tau_0.10=0.6860468604251301`, `best_val_pred_to_gt_p90=0.20770130679011345`, and `best_val_chamfer_l2=0.026904070439438026`;
- conclusion: WAN Route2 is live but setting-limited; current T2V noisy-denoising hidden states are not competitive geometry features under the fixed adapter setting.

WAN sanity job `86316` completed and confirmed that the WAN feature cache path is non-degenerate:

- the feature-sensitivity sweep shows the real WAN caches differ materially from both the zero-feature and sample-shuffle controls;
- mean feature relative L2 to the reference config is `1.4833955196788304`;
- mean adapter-token relative L2 to the reference config is `0.9976405603462312`;
- zero-control `t499/layer09` reached `best_val_fscore_tau_0.10=0.38500468702435137`, `best_val_chamfer_l2=0.8643565624952316`, and `best_val_pred_to_gt_p90=0.47307824591795605`;
- sample-shuffle `t499/layer09` reached `best_val_fscore_tau_0.10=0.37489499366133267`, `best_val_chamfer_l2=0.682008907198906`, and `best_val_pred_to_gt_p90=0.6679268131653467`.

Current interpretation:

- WAN is not trivially broken and does react to the selected video-context features;
- however, the absolute scores are still far below the clean-GT VGGT default, so the current Route2 branch should remain a setting-sensitive exploratory comparison rather than a claim-level conclusion;
- `86307` has finished; the next audit should change the WAN feature extraction setting before changing the SCRREAM GT, split, adapter, or NOVA decoder.

Route2.1 implementation / queue state at `2026-05-12 22:47 CST` superseded by the 2026-05-15 update above:

- `prepare_scrream_wan_t2v_feature_cache.py` now implements `noise_mode=normal|no_noise|low_noise`;
- `no_noise` uses the clean WAN VAE latent and scheduler index `999` only for the timestep embedding;
- `low_noise` adds scheduler noise at index `999` and uses the matching low-noise timestep embedding;
- cache metadata records `noise_mode`, `requested_timestep_index`, `scheduler_timestep`, `latent_noise_applied`, and `low_noise_index`;
- `train_wan_t2v_nova_adapter.py` persists representative WAN cache metadata into config / `final_metrics.json`;
- queued Route2.1 chain was originally cache smokes `86342` (`low_noise`) / `86343` (`no_noise`), dependent full caches `86350` / `86351`, no-noise smoke train `86357`, and formal packs `86358` / `86359`; the formal packs were later replaced with 1-GPU jobs `86371` / `86372` to use available fragmented GPUs;
- first grid remains layers `9,14,29` with `no_noise` and `low_noise`, using the same clean SCRREAM `.pt`, full val split, robust metrics, and `val_visual_40960/`;
- run one targeted `t499/layer09 + token_layernorm` setting to test whether feature scale / normalization is masking WAN quality;
- then run `pair_tiled81` vs `ctx81` and tensor-choice audits. This was later superseded by the 2026-05-16 update above.

## Update — 2026-05-11 17:54 CST

Clean-GT VGGT rerun `86286` finished successfully:

- Slurm job `86286` / `scrream_vggt_layer_pack`: `COMPLETED`, exit `0:0`, elapsed `02:23:11`, node `air-node-04`
- output prefix: `experiments/probe3d/result/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_mlp_l4_nova_flow_robustval_metafilter_seed17_vggt_layerXX`
- this remains a 30-epoch-equivalent `9510`-step comparison because it was submitted before the default changed to 50 epochs

Current clean-GT VGGT interpretation:

| layer | best F@0.10 ↑ | best pred→GT p90 ↓ | best Chamfer-L2 ↓ | note |
| ---: | ---: | ---: | ---: | --- |
| 16 | **0.6860468604251301** | **0.20770130679011345** | **0.026904070439438026** | current default |
| 24 | 0.6802513448244976 | 0.25152526050806046 | 0.03554012098660072 | closest comparison; stronger final recall |
| 20 | 0.548158719135383 | 0.36711231619119644 | 0.0767408860847354 | no longer default under clean GT |

The old layer `20` claim from job `86149` is now pre-meta-filter history. Under the sequence-meta-filtered GT, layer `16` is the current default representation, and layer `24` is the main comparison point to inspect visually.

WAN Route2 execution state:

- full feature precompute completed:
  - `86292` timestep `249`: `COMPLETED`, exit `0:0`, elapsed `01:01:18`
  - `86293` timestep `499`: `COMPLETED`, exit `0:0`, elapsed `00:59:44`
  - `86294` timestep `749`: `COMPLETED`, exit `0:0`, elapsed `00:59:36`
- cache root: `experiments/probe3d/feature_cache/scrream_wan_t2v1p3b_ctx81`
- cache size: `4937` `.pt` files / about `45G`
- official grid coverage: `329` samples for each timestep `249,499,749` and code layer `9,14,19,24,29`; the extra `t749/layer20` files are a 2-sample historical smoke cache
- first full training pack attempt `86306` failed immediately with `AttributeError: 'float' object has no attribute 'item'` at final scalar reporting
- fix applied in `experiments/probe3d/train_wan_t2v_nova_adapter.py`: `final_loss = reduced_loss`
- failed output was renamed to `...wan_t2v1p3b_ctx81_t249_layer09_failed_86306`
- replacement training pack `86307` was running on `air-node-04`, `1 x A100`, `WAN_PACK_PARALLEL=1`, 50 epochs / `15850` steps per run
- at this timestamp `t249/layer09` and `t249/layer14` completed and `t249/layer19` was running

Immediate next checks:

```bash
squeue -j 86307 -o '%.18i %.9P %.32j %.8T %.10M %.10l %.6D %R'
tail -80 slurm_out/scrream_wan_t2v_pack_86307.out
tail -80 slurm_out/scrream_wan_t2v_t249_layer14_86307.log
```

## Update — 2026-05-11 02:53 CST

SCRREAM mesh-complete GT was corrected after discovering that reduced sequences could inherit phantom objects from scene-level `meshes/*.obj`. The data bridge now reads the current sequence `meta.txt` and samples only the listed object meshes before frustum cropping.

Clean data regeneration:

- Slurm job `86283` / `scrream_mesh_prep`: `COMPLETED`, exit `0:0`, elapsed `00:51:13`, node `air-node-02`
- Slurm job `86284` / `scrream_split_merge`: `COMPLETED`, exit `0:0`, elapsed `00:00:29`, node `air-node-02`
- formal data: `experiments/probe3d/adapter_data/scrream_mesh_complete_n2_adapter_seed17_tp20000_ms500000_trainplus_test.pt`
- shape / split: `[329, 20000, 3]`, `train=317`, `val=12`
- global meta: `mesh_sequence_meta_filter=True`
- old pre-meta-filter `.pt` files moved to `experiments/probe3d/adapter_data/deprecated_meta_filter_bug/`
- mesh cache was cleared and rebuilt with sequence/meta-hash labels, not scene-only labels

Sanity check on the known bad sample:

- sample: `scene08/scene08_reduced_00_000220_000260`
- selected mesh count: `22`
- excluded by `meta.txt`: `human-female_mannequin_green_top.obj`, `human-female_mannequin_grey_top.obj`, `human-male_mannequin_colorful_top.obj`

Clean-GT VGGT rerun:

- Slurm job `86285` requested `4 x A100` but was cancelled because it would wait for resources too long.
- At this timestamp, Slurm job `86286` was running on `air-node-04` with `2 x A100`; it later completed successfully as recorded in the 2026-05-11 17:54 CST update.
- output prefix: `experiments/probe3d/result/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_mlp_l4_nova_flow_robustval_metafilter_seed17_vggt_layerXX`
- job `86286` was submitted before the default epoch change and still uses `9510` steps, equivalent to 30 epochs at `train=317`, batch size 1.

WAN Route2 update:

- WAN checkpoint is present under `checkpoints/wan2.1/Wan2.1-T2V-1.3B-Diffusers` (~27 GB).
- 2-sample feature smoke job `86282` completed successfully for timestep `749`, layer `20`.
- Smoke cache files exist under `experiments/probe3d/feature_cache/scrream_wan_t2v1p3b_ctx81/t749/layer20/`.
- At this timestamp, full WAN precompute/training was still gated on a one-step WAN adapter training smoke; this was later superseded by the completed full precompute and running job `86307` recorded above.

Training-length policy:

- formal SCRREAM VGGT/WAN ablation defaults are now 50 epochs.
- current `86286` remains a 30-epoch-equivalent rerun because Slurm computed `max_steps=9510` at submit time.

## Update — 2026-05-10 21:17 CST

Current repo / machine state at that point:

- active Git branch: `master`
- new third-party submodule: `third_party/VidFM3D` -> `https://github.com/zxhuang1698/VidFM3D.git`
- WAN Route2 dependency file: `experiments/probe3d/requirements-wan-t2v.txt`
- WAN feature precompute script: `experiments/probe3d/scripts/prepare_scrream_wan_t2v_feature_cache.py`
- WAN training script: `experiments/probe3d/train_wan_t2v_nova_adapter.py`
- WAN Slurm scripts:
  - `slurm/scrream_wan_t2v_download.sbatch`
  - `slurm/scrream_wan_t2v_precompute.sbatch`
  - `slurm/scrream_wan_t2v_ablation_pack_train.sbatch`

WAN Route2 keeps the exact SCRREAM VGGT ablation setting fixed:

- adapter data: `experiments/probe3d/adapter_data/scrream_mesh_complete_n2_adapter_seed17_tp20000_ms500000_trainplus_test.pt`
- split: `train=317`, `val=12`
- GT: registered SCRREAM mesh-complete target, `target_points=20000`, `mesh_sample_points=500000`, two-view union-frustum crop, first input camera frame
- adapter/loss/decoder: `MLP-L4-H1024`, `nova_flow`, `num_queries=20000`, NOVA `scene_ae`
- validation: full val split, robust metrics, `val_preview_queries=40960`, `val_visual_40960/`

Only the representation changes. WAN T2V is loaded during feature precompute and cached per sample; training reads cache files and does not run WAN in every adapter step. The planned ablation grid is:

- model: `Wan-AI/Wan2.1-T2V-1.3B-Diffusers`
- context: 81 RGB frames around each SCRREAM pair
- prompt: empty string
- timesteps: `249,499,749`
- code layer ids: `9,14,19,24,29`
- cache tensor shape per sample: `[3120,1536]`

WAN dependency / preflight status:

- `experiments/probe3d/requirements-wan-t2v.txt` pins `transformers>=4.49.0,<5.0` and `huggingface-hub[cli]>=0.34,<1.0` to avoid the incompatible `transformers 5.8.0` / `huggingface_hub 1.x` path.
- In conda env `nova3r`, the verified imports are `diffusers 0.37.1`, `transformers 4.57.6`, `tokenizers 0.22.2`, real `diffusers.pipelines.wan.pipeline_wan.WanPipeline`, and real `transformers.models.umt5.modeling_umt5.UMT5EncoderModel`.
- A window-only SCRREAM pass validated all `329` 81-frame windows without loading WAN.
- The compute node cannot reach login-node loopback `127.0.0.1:17890` directly, so WAN Slurm scripts create an SSH local tunnel to `air-server:127.0.0.1:17890` by default. Set `WAN_DISABLE_PROXY=1` only when the job environment has direct network access.
- Historical at this timestamp: WAN checkpoint download/preflight job `86267` had not completed yet on `air-node-02`, with about `14G` downloaded. Superseded by the 2026-05-11 update above: the checkpoint is now complete and the 2-sample feature smoke passed.

Historical WAN command after the completed checkpoint and 2-sample feature smoke:

```bash
WAN_TIMESTEPS=749 WAN_LAYERS=20 WAN_MAX_STEPS=1 WAN_VAL_PREVIEW_SAMPLES=2 \
  sbatch slurm/scrream_wan_t2v_ablation_pack_train.sbatch
```

This is superseded by the 2026-05-12 update above: full precompute completed, the scalar-reporting bug was fixed, WAN sanity controls completed, and the full WAN training pack completed as job `86307`.

## Environment

- repo root: `/home/JJ_Group/lih2511/3dprobe`
- current branch at 2026-05-10 update: `master`
- conda env: `nova3r`
- full SCRREAM root: `~/datasets/SCRREAM`
- Slurm scripts: `slurm/`
- Slurm logs: `slurm_out/`
- non-WAN network proxy for Slurm / HuggingFace / SwanLab access: `http://127.0.0.1:7896`
- WAN repo/checkpoint proxy: `http://127.0.0.1:17890` through the SSH tunnel logic in `slurm/scrream_wan_t2v_*.sbatch`
- SwanLab: installed in `nova3r`; the API key is sourced from ignored local file `slurm/.secrets.env`

Do not launch full data generation or training directly in an interactive shell. Use `sbatch`.

## Third-party source state

This branch uses Git submodules for third-party model code:

- `third_party/vggt` -> `https://github.com/facebookresearch/vggt.git`
- `third_party/Wan2.1` -> `https://github.com/Wan-Video/Wan2.1.git`
- `third_party/VidFM3D` -> `https://github.com/zxhuang1698/VidFM3D.git`

After a fresh clone, run:

```bash
git submodule update --init --recursive
```

Important operational note: `third_party/vggt` must be initialized before adapter training. If it is an empty directory, `train_vggt_nova_adapter.py` fails with `ModuleNotFoundError: No module named 'vggt.models.vggt'`.

Wan2.1 / VidFM3D dependencies are not merged into the root `requirements.txt` or `environment.yml`. For the SCRREAM WAN Route2 probe, use `experiments/probe3d/requirements-wan-t2v.txt` inside `nova3r`; the training code path remains separate from `train_vggt_nova_adapter.py`.

## Current SCRREAM data products

All current formal SCRREAM adapter data uses `target_source=mesh_complete`:

1. Read two-view pairs from `data/scrream/scrream_n2_list.json`.
2. Use the two pair RGB frames as VGGT adapter inputs.
3. Read the current sequence `meta.txt` and select only matching registered `sceneXX/meshes/*.obj`.
4. Sample selected mesh surfaces proportional to surface area.
5. Voxel-deduplicate and cache per-sequence/meta-hash mesh reservoirs under `experiments/probe3d/adapter_data/mesh_cache/`.
6. Crop mesh points to the union frustum of the two input views.
7. Transform the target to the first input camera coordinates.
8. FPS / pad to a fixed target count.

Current generated `.pt` datasets:

| File | Shape | Split |
| --- | --- | --- |
| `experiments/probe3d/adapter_data/scrream_mesh_complete_n2_adapter_seed17.pt` | `[329, 10000, 3]` | train `223`, val `12`, test `94` |
| `experiments/probe3d/adapter_data/scrream_mesh_complete_n2_adapter_seed17_trainplus_test.pt` | `[329, 10000, 3]` | train `317`, val `12` |
| `experiments/probe3d/adapter_data/scrream_mesh_complete_n2_adapter_seed17_tp20000_ms500000.pt` | `[329, 20000, 3]` | train `223`, val `12`, test `94` |
| `experiments/probe3d/adapter_data/scrream_mesh_complete_n2_adapter_seed17_tp20000_ms500000_trainplus_test.pt` | `[329, 20000, 3]` | train `317`, val `12`; `mesh_sequence_meta_filter=True` |

Original scene-level split:

- train: `scene01`, `scene02`, `scene03`, `scene04`, `scene05`, `scene07`, `scene09`
- val: `scene08`
- test: `scene10`

The `trainplus_test` variants are split-label rewrites only: they convert `test -> train` and keep `val`, so there is no held-out test split in those files.

The `scrream_official_depth_mix_*` files under `experiments/probe3d/adapter_data/` are historical local ablation artifacts. They are not part of the current training line and should be ignored unless the user explicitly asks for a depth-mix ablation.

## Completed 20k baseline training

Job `86140` completed successfully:

- job name: `scrream_mesh_mlp`
- state: `COMPLETED`
- exit code: `0:0`
- node: `air-node-02`
- GPU: `1 x A100`
- elapsed: `00:26:26`
- Slurm start time: `2026-05-07T22:13:56`
- Slurm end time: `2026-05-07T22:40:22`
- input adapter data:
  - `experiments/probe3d/adapter_data/scrream_mesh_complete_n2_adapter_seed17_tp20000_ms500000_trainplus_test.pt`
- output dir:
  - `experiments/probe3d/result/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_mlp_l4_nova_flow_seed17`
- feature cache:
  - `experiments/probe3d/feature_cache/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_vggt23`
- SwanLab run:
  - `https://swanlab.cn/@JiachengDong/PSUVPSC3DD/runs/eoiupi3bv2g11dvd21ypm`

Training config:

- adapter: `MLP-L4`, hidden dim `1024`
- loss: `nova_flow`
- views: `2`
- `target_points=20000`
- `mesh_sample_points=500000`
- `num_queries=20000`
- train samples: `317`
- val samples: `12`
- requested length: `9510` steps, equivalent to `317 * 30` single-GPU steps

Final metrics from `final_metrics.json`:

- `first_loss=1.4599288702011108`
- `final_loss=0.8398033976554871`
- `best_loss=0.5998285412788391`
- `best_val_chamfer_l2=0.5149603486061096`

The latest `validation_metrics.json` is:

- step: `9500`
- `val_chamfer_l2=0.6779176592826843`

The best validation value came earlier and is stored in `final_metrics.json`. Checkpoints were written every 500 steps through `step_009500.pth`, plus `best.pth` and `latest.pth`.

## Completed robust VGGT layer ablation

Pre-meta-filter job `86149` completed successfully:

- job name: `scrream_vggt_layer_pack`
- state: `COMPLETED`
- exit code: `0:0`
- GPU request: `4 x A100`
- elapsed: `02:00:08`
- Slurm start time: `2026-05-08T01:26:42`
- Slurm end time: `2026-05-08T03:26:50`
- input adapter data:
  - `experiments/probe3d/adapter_data/scrream_mesh_complete_n2_adapter_seed17_tp20000_ms500000_trainplus_test.pt`
- output prefix:
  - `experiments/probe3d/result/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_mlp_l4_nova_flow_robustval_seed17_vggt_layerXX`

This run used the new robust validation path:

- full val split (`eval_batches=0`, 12 samples);
- `val_metric_max_points=20000`;
- visual predictions with `val_preview_queries=40960`;
- checkpoint selection by legacy Chamfer, `val_fscore_tau_0.10`, and `val_pred_to_gt_p90`.

Final pre-meta-filter layer comparison:

| layer | F@0.10 ↑ | pred→GT p90 ↓ | GT→pred p90 ↓ | trimmed CD95 ↓ | Chamfer ↓ | velocity MSE ↓ |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 0 | 0.360445 | 0.757063 | 0.217211 | 0.160015 | 0.813673 | 1.219393 |
| 4 | 0.426661 | 0.523990 | 0.295402 | 0.091500 | 0.545035 | 1.234664 |
| 8 | 0.430191 | 0.513064 | 0.209427 | 0.079848 | 0.271475 | 1.229198 |
| 12 | 0.467699 | 0.430590 | 0.236178 | 0.063575 | 0.100242 | 1.213175 |
| 16 | 0.649718 | **0.207854** | 0.187817 | 0.021746 | 0.034842 | 1.166274 |
| 20 | **0.672748** | 0.231753 | **0.158520** | **0.020939** | **0.034735** | **1.154195** |
| 24 | 0.661291 | 0.241899 | 0.170757 | 0.022651 | 0.038710 | 1.171148 |

Historical interpretation before the sequence-meta filter:

- VGGT layer `20` was the best default probe layer under the contaminated scene-level GT.
- VGGT layer `16` is the main qualitative comparison if prediction-side outliers are more important than coverage.
- Layers `0/4/8/12` are clearly weaker than the late VGGT representations.
- Visual review should compare layer `16`, `20`, and `24` under `val_visual_40960/step_009500/`.

This run is now historical because it used the scene-level all-object mesh reservoirs. Use the clean-GT rerun `86286` before making new layer-ranking claims.

The SCRREAM data bridge is at V0.5: the mesh-complete bridge and training loop are wired end to end, but the next research direction is to increase training data scale and test more model variants rather than treat the current 329-pair setup as final.

## Slurm training scripts

`slurm/scrream_mesh_complete_mlp_train.sbatch` now supports single-node multi-GPU training:

- one visible GPU: launches normal `python`
- multiple visible GPUs: launches `torchrun --standalone --nnodes=1 --nproc_per_node=$SCRREAM_GPUS_PER_NODE`

For future multi-GPU runs, prefer setting `SCRREAM_EPOCHS` instead of manually computing `SCRREAM_MAX_STEPS`; the script computes:

```text
max_steps = ceil(train_count * SCRREAM_EPOCHS / (batch_size_per_gpu * gpu_count))
```

Example four-GPU launch:

```bash
SCRREAM_GPUS_PER_NODE=4 \
SCRREAM_EPOCHS=50 \
SCRREAM_ADAPTER_DATA=experiments/probe3d/adapter_data/scrream_mesh_complete_n2_adapter_seed17_tp20000_ms500000_trainplus_test.pt \
SCRREAM_OUTPUT_DIR=experiments/probe3d/result/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_mlp_l4_nova_flow_seed17_ddp4 \
SCRREAM_FEATURE_CACHE_DIR=experiments/probe3d/feature_cache/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_vggt23 \
SCRREAM_SWANLAB=1 \
SCRREAM_NUM_QUERIES=20000 \
sbatch --qos=high --nodelist=<node-with-free-a100s> --gres=gpu:a100:4 --mem=96G \
  slurm/scrream_mesh_complete_mlp_train.sbatch
```

Before submitting a future training run, check available GPUs and memory:

```bash
sinfo -N -o '%N %P %t %G %C %m'
squeue -o '%.18i %.20u %.30j %.10T %.18P %.20R %.10M %.8D %.20b'
scontrol show node air-node-02 | grep -E 'NodeName=|RealMemory=|AllocTRES=|CfgTRES=|State='
scontrol show node air-node-03 | grep -E 'NodeName=|RealMemory=|AllocTRES=|CfgTRES=|State='
```

Use `--qos=high` when appropriate; this was needed on 2026-05-07 to start the current job despite idle GPUs.

## Split merge helper

`slurm/scrream_merge_test_into_train.sbatch` converts `test` samples into `train`, preserves `val`, writes a `.manifest.json`, and runs `check_scrream_adapter_training_inputs.py` for both train and val.

Example:

```bash
SCRREAM_SOURCE_ADAPTER_DATA=experiments/probe3d/adapter_data/scrream_mesh_complete_n2_adapter_seed17_tp20000_ms500000.pt \
SCRREAM_ADAPTER_OUT=experiments/probe3d/adapter_data/scrream_mesh_complete_n2_adapter_seed17_tp20000_ms500000_trainplus_test.pt \
SCRREAM_NUM_QUERIES=20000 \
sbatch slurm/scrream_merge_test_into_train.sbatch
```

## Next checklist

1. Use old WAN hidden `t499/layer09`, historical CA `t499/layer14`, repeated CA `t249/layer09/14`, negative multi-layer fusion, negative multi-timestep fusion, negative FLF2V hidden, and clean-GT VGGT layer `16` as the primary comparison baselines.
2. Hidden CA stability/capacity jobs `86547`-`86554` have completed. Use L4 `t249/layer14` as the latest small positive, but keep historical `t499/layer14` and repeated `t249/layer09/14` as the main comparison anchors.
3. Remember FLF2V is not T2V `ctx81`: use `pair_endpoint81`, first/last-frame conditioning, temporal indices `[0,20]`, and output prefix `wan_flf2v14b_pair_endpoint81`. The first hidden run was negative, so do not expand FLF2V hidden broadly unless later tensor-choice evidence motivates it.
4. Inspect CA-resampler full-grid visuals for `t249/layer09` and `t249/layer14` before spending more queue time on repeat checks.
5. Increase SCRREAM training sample scale beyond the current 329 official pairs after the WAN interface/tensor-choice audit.
6. Keep robust validation metrics and `val_visual_40960/` exports enabled for all formal Slurm runs.
7. Do not use `scrream_official_depth_mix_*` artifacts in the current narrative unless explicitly running an ablation.
