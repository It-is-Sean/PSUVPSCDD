# Agent Notes

This repo is the active PSUVPSC3DD / NOVA3R probe workspace. Start from these files before changing experiment logic:

- `README.md`
- `PROJECT.md`
- `experiments/probe3d/README.md`
- `docs/probe/handoff_2026-05-07.md`
- `docs/probe/handoff_2026-05-03.md`
- `docs/probe/experiment_plan.md`
- `docs/probe/todo.md`

Current execution rules:

- Use conda env `nova3r`.
- Long data preparation and training jobs run through Slurm scripts in `slurm/`.
- Slurm logs go to `slurm_out/`.
- Do not run full SCRREAM data generation or training directly in an interactive shell.
- Non-WAN network-dependent Slurm jobs should default to `HTTP_PROXY`, `HTTPS_PROXY`, and `ALL_PROXY` at `http://127.0.0.1:7896`.
- WAN Route2 / FLF2V repo/checkpoint jobs use `http://127.0.0.1:17890`; on compute nodes the WAN Slurm scripts create an SSH local tunnel back to `air-server:127.0.0.1:17890`.

Current SCRREAM adapter branch:

- Full SCRREAM data root is `~/datasets/SCRREAM`.
- Old local `eval_scrream` runs are invalid for formal claims.
- Active GT source is `mesh_complete`: read the current sequence `meta.txt`, sample only the listed registered `sceneXX/meshes/*.obj` surfaces proportional to surface area, crop to the two-input-view union frustum, transform to the first input camera frame, then FPS/pad to fixed target points.
- Adapter `.pt` generation script is `experiments/probe3d/scripts/prepare_scrream_full_adapter_data.py`.
- Training entrypoint is `experiments/probe3d/train_vggt_nova_adapter.py --dataset scrream_adapter --data_root <adapter.pt>`.
- The SCRREAM `nova_flow` path applies the NOVA decoder checkpoint target `norm_mode`; current `scene_ae` config reports `norm_mode=median_3`.
- Ignore local `scrream_official_depth_mix_*` adapter artifacts unless the user explicitly asks for a depth-mix ablation.

Current machine state recorded on 2026-05-19 13:13 CST:

- downloaded checkpoints are under `checkpoints/scene_n1/`, `checkpoints/scene_n2/`, `checkpoints/scene_ae/`, `checkpoints/vggt/model.pt`, `checkpoints/wan2.1/Wan2.1-T2V-1.3B-Diffusers`, and `checkpoints/wan2.1/Wan2.1-FLF2V-14B-720P-diffusers`;
- SwanLab is installed in `nova3r`;
- current branch is `master`;
- `third_party/vggt`, `third_party/Wan2.1`, and `third_party/VidFM3D` are Git submodules; run `git submodule update --init --recursive` after a fresh clone;
- current formal data is `experiments/probe3d/adapter_data/scrream_mesh_complete_n2_adapter_seed17_tp20000_ms500000_trainplus_test.pt` with shape `[329, 20000, 3]`, split `train=317`, `val=12`, and `mesh_sequence_meta_filter=True`;
- old pre-meta-filter `.pt` files are archived under `experiments/probe3d/adapter_data/deprecated_meta_filter_bug/`;
- SCRREAM data bridge status is V0.5: mesh-complete GT from registered SCRREAM meshes is wired end to end for adapter training, but future work should expand training data scale and model coverage;
- robust VGGT layer ablation job `86149` completed successfully on `2026-05-08`, but it used the pre-meta-filter GT and should be treated as historical;
- clean-GT VGGT layer ablation job `86286` completed successfully on `air-node-04` with exit `0:0`, elapsed `02:23:11`; outputs use prefix `experiments/probe3d/result/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_mlp_l4_nova_flow_robustval_metafilter_seed17_vggt_layerXX`;
- current clean-GT VGGT default is layer `16` (`best_val_fscore_tau_0.10=0.6860468604251301`, `best_val_pred_to_gt_p90=0.20770130679011345`, `best_val_chamfer_l2=0.026904070439438026`); layer `24` is the main comparison point, and old layer `20` is historical/pre-meta-filter or a weaker clean-GT result;
- visual validation outputs are under `experiments/probe3d/result/*robustval*/val_visual_40960/`;
- `slurm/scrream_mesh_complete_mlp_train.sbatch` supports single-node DDP via `SCRREAM_GPUS_PER_NODE`; `slurm/scrream_vggt_layer_ablation_pack_train.sbatch` is the preferred packed layer-ablation launcher.
- WAN Route2 implementation exists: feature precompute is `experiments/probe3d/scripts/prepare_scrream_wan_t2v_feature_cache.py`, training is `experiments/probe3d/train_wan_t2v_nova_adapter.py`, dependencies are isolated in `experiments/probe3d/requirements-wan-t2v.txt`, and Slurm launchers are `slurm/scrream_wan_t2v_download.sbatch`, `slurm/scrream_wan_t2v_precompute.sbatch`, `slurm/scrream_wan_t2v_ablation_pack_train.sbatch`, `slurm/scrream_wan_t2v_route21_pack_train.sbatch`, `slurm/scrream_wan_t2v_sanity.sbatch`, `slurm/scrream_wan_t2v_multilayer_pack_train.sbatch`, and `slurm/scrream_wan_t2v_multitime_pack_train.sbatch`.
- WAN Route2 keeps the clean SCRREAM mesh-complete `.pt`, split, MLP adapter, NOVA decoder, and robust validation setup from the VGGT ablation, but replaces VGGT features with cached WAN2.1 T2V video-context features from 81-frame windows. The formal grid is timesteps `249,499,749` x code layers `9,14,19,24,29`; checkpoint download, window validation, 2-sample feature smoke, and full feature precompute have completed.
- WAN feature cache root is `experiments/probe3d/feature_cache/scrream_wan_t2v1p3b_ctx81`, currently `4937` `.pt` files / about `45G`; official grid files are complete at `329` samples per `(timestep, layer)` plus a 2-file historical smoke cache at `t749/layer20`.
- WAN sanity job `86316` completed; the real WAN features differ materially from zero/sample-shuffle controls, so the cache path is live, but the route is still much weaker than clean-GT VGGT and may be setting-limited.
- WAN training pack job `86306` failed immediately due a reduced-loss float `.item()` bug; `experiments/probe3d/train_wan_t2v_nova_adapter.py` has been patched so `final_loss = reduced_loss`. Replacement job `86307` completed on `air-node-04` with exit `0:0`, elapsed `13:36:04`, `1 x A100`, serial `WAN_PACK_PARALLEL=1`, 50 epochs / `15850` steps per run.
- WAN Route2 best formal run is `t499/layer09` with `best_val_fscore_tau_0.10=0.46988987902779306`, `best_val_pred_to_gt_p90=0.48718947172164917`, and `best_val_chamfer_l2=0.21040735269586244`; this is above zero/sample-shuffle controls but far below clean-GT VGGT layer `16` (`F@0.10=0.6860468604251301`, `pred_to_gt_p90=0.20770130679011345`, `Chamfer=0.026904070439438026`).
- WAN Route2.1 clean / low-noise audit has completed: smoke cache jobs `86342` / `86343`, full cache jobs `86350` / `86351`, smoke train `86357`, and replacement formal packs `86371` / `86372` all exited `0:0`; no-noise and low-noise caches are complete for layers `9,14,29` at `329` samples each.
- Route2.1 did not beat old WAN Route2 best `t499/layer09`: best clean/low-noise result is `no_noise/layer29` with `best_val_fscore_tau_0.10=0.44840173` and `best_val_pred_to_gt_p90=0.48779308`, below old Route2 `F@0.10=0.46988987902779306`.
- `experiments/probe3d/train_wan_t2v_nova_adapter.py` supports `--wan_feature_norm none|token_layernorm|sample_standardize|token_l2`; targeted normalization job `86395` completed with `F@0.10=0.45476213575933216`, `pred_to_gt_p90=0.484821617603302`, and `Chamfer=0.1771892917652925`, so token normalization did not beat old Route2 best.
- `pair_tiled81` vs `ctx81` completed as jobs `86396 -> 86397 -> 86400 -> 86401`; result `F@0.10=0.4429200036613287`, `pred_to_gt_p90=0.5104739194115003`, `Chamfer=0.16478415516515574`, so replacing real video context with tiled pair frames did not fix WAN.
- WAN latent tensor-choice probing is implemented for `--feature_kind pred_x0_latent` and `--feature_kind model_output_latent`. `pred_x0_latent` writes `t499/pred_x0_latent/<sample>.pt` using `x0 = sample - sigma * model_output`; full cache job `86411` completed with `329/329` samples and feature shape `[12480,16]`. `model_output_latent` writes raw WAN transformer `output.sample` latent slices under `t499/model_output_latent/<sample>.pt`; cache job `86534` and smoke job `86535` completed.
- Original pred-x0 MLP readout is not a valid completed result: smoke job `86409` failed because the 2-sample cache did not cover the sampled train batch, and formal job `86412` failed on the first backward with a PyTorch CUDA `AdaptiveAveragePooling` shared-memory assert from pooling `[12480,128] -> [768,128]`.
- Latent readouts currently include `WanPredX0LatentConvAdapter` / `--adapter_type conv2d_mlp` and `WanLatentCrossAttentionResamplerAdapter` / `--adapter_type wan_latent_cross_attn_resampler`. The Conv2d path reshapes `[B,12480,16]` to `[B,2,60,104,16]`, Conv2d stride-2s to `[B,3120,128]`, then pools to `[B,768,128]`. The latent CA path restores `[B,2,60,104,16]`, adds temporal/row/column position embeddings, and cross-attends `768` NOVA query tokens to the latent grid. Both are valid for `pred_x0_latent` and `model_output_latent`.
- Pred-x0 conv formal job `86422` completed on `air-node-02`, exit `0:0`, elapsed `00:52:13`: `best_val_fscore_tau_0.10=0.41336424426176693`, `best_val_pred_to_gt_p90=0.6272750149170557`, `best_val_chamfer_l2=0.35522504647572833`. It did not beat old WAN hidden `t499/layer09`.
- Model-output latent Conv2d branch: full cache `86534` has `329/329` files under `experiments/probe3d/feature_cache/scrream_wan_t2v1p3b_ctx81_model_output_latent/t499/model_output_latent`, feature shape `[12480,16]`, metadata `model_output_formula=transformer_output_sample`; smoke `86535` and formal `86536` completed `0:0`. Formal result: `F@0.10=0.4199299775478907`, `pred_to_gt_p90=0.6285491685072581`, `Chamfer=0.44791099180777866`.
- Learned latent CA branch: local shape/backward smoke passed for `[1,12480,16] -> [1,768,128]`; model-output smoke `86539` and formal `86540` completed `0:0`; high-memory pred-x0 smoke `86541` was cancelled and replaced by lower-memory smoke `86542`, which completed `0:0`; formal pred-x0 learned-CA job `86543` completed `0:0`. Results: `model_output_latent + latent CA` F@0.10 `0.4289946537162989`, p90 `0.5942087918519974`, Chamfer `0.19963246708114943`; `pred_x0_latent + latent CA` F@0.10 `0.43766060222664477`, p90 `0.5175856028993925`, Chamfer `0.20880577837427458`. Latent tensor-choice readouts did not beat hidden CA, so stop this branch for now.
- WAN hidden structured readout audits are implemented in `experiments/probe3d/probe/adapter.py` and `train_wan_t2v_nova_adapter.py`: `--adapter_type grid2d_pool` maps hidden `[B,3120,1536]` as `[B,2,30,52,1536]` to `[B,768,128]` via 2D pooling, and `--adapter_type grid2d_conv` adds a small same-resolution 3x3 Conv2d readout before pooling. Both are restricted to `--wan_feature_kind hidden`.
- Grid readout jobs completed on `air-node-04`: `grid2d_pool` smoke/formal `86423` / `86424` yielded `F@0.10=0.43326753863230877`, `pred_to_gt_p90=0.5651116619507471`, `Chamfer=0.1759416777640581`; `grid2d_conv` smoke/formal `86425` / `86426` yielded `F@0.10=0.39855373112050535`, `pred_to_gt_p90=0.7689011543989182`, `Chamfer=1.159957120815913`. Simple structured pooling/conv does not explain the WAN gap.
- WAN learned readout is implemented as `--adapter_type wan_cross_attn_resampler`: it uses hidden cache `[B,3120,1536]` as `[B,2,30,52,1536]`, adds temporal/row/column position embeddings, and cross-attends `768` learnable NOVA query tokens to WAN tokens. Smoke/formal `t499/layer09` jobs `86427` / `86428` completed; layer09 nearly matched old WAN hidden with `F@0.10=0.4675106769755634`, `pred_to_gt_p90=0.4791356101632118`, `Chamfer=0.1388903713474671`.
- WAN CA-resampler layer sweep job `86429` completed `0:0` on `air-node-02`; `t499/layer14` is the historical best WAN run with `F@0.10=0.5012956284974035`, `pred_to_gt_p90=0.4229188362757365`, `Chamfer=0.10908368105689685`. User inspected `val_visual_40960/step_011500/` and confirmed the visual result improved. Layer sweep F@0.10: layer09 `0.46751068`, layer14 `0.50129563`, layer19 `0.45041048`, layer24 `0.40883680`, layer29 `0.38334162`.
- CA-resampler stability / full-grid jobs completed: old pending job `86444` was cancelled and replaced by low-LR job `86453`, which completed `0:0` on `air-node-02` but was negative (`t499/layer14`, `WAN_LR=5e-5`, `F@0.10=0.4292316005720653`, `pred_to_gt_p90=0.5338096991181374`, `Chamfer=0.15617707930505276`). Full-grid job `86468` completed `0:0` on `air-node-02`, elapsed `06:31:56`, for `249/499/749 x 9/14/19/24/29` with CA resampler; full-grid best F is `t249/layer14` (`0.48913902331806663`), and full-grid best p90 / Chamfer is `t249/layer09` (`0.410525918006897` / `0.11981592203179996`).
- WAN multi-source hidden fusion is implemented in `experiments/probe3d/probe/adapter.py` as `WanHiddenMultiSourceCrossAttentionResamplerAdapter`, with CLI adapters `--adapter_type wan_cross_attn_multilayer_resampler` and `--adapter_type wan_cross_attn_multitime_resampler`. `train_wan_t2v_nova_adapter.py` now accepts `--wan_layers "9,14"` and `--wan_timesteps "249,499,749"` for hidden-cache fusion; it forbids mixing multi-layer and multi-timestep modes in the same run and records `wan_layers`, `wan_timesteps`, and cache metadata in configs/metrics.
- WAN multi-layer fusion jobs: smoke `86483` completed; bad submit `86484` and high-QOS blocked job `86486` were cancelled; formal `t249 layers 9+14` job `86485` completed `0:0` with `F@0.10=0.46487238144059545`, `pred_to_gt_p90=0.49916083614031476`, `Chamfer=0.12842474008599916`; formal `t249 layers 9+14+19` job `86487` finished training and wrote metrics/checkpoints but Slurm marked `FAILED 1:0` only because `swanlab.finish()` hit a proxy error after writeout, with `F@0.10=0.4724058254433277`, `pred_to_gt_p90=0.449202927450339`, `Chamfer=0.12805132629970709`. Simple multi-layer hidden fusion did not beat `t249/layer14` or `t249/layer09`, so stop this branch for now.
- `train_wan_t2v_nova_adapter.py` now wraps `swanlab.finish()` in `try/except` and prints `SwanLab finish warning` instead of failing a completed training run on upload/proxy teardown.
- WAN multi-timestep same-layer fusion: smoke job `86493` completed `0:0` for timesteps `249+499+749`, layer `14`, input shape `[1,9360,1536]`; formal job `86494` completed `0:0` with `F@0.10=0.45213117002164466`, `pred_to_gt_p90=0.49119475732247037`, and `Chamfer=0.12659800921877226`. This did not beat single-layer CA baselines, so stop simple hidden-fusion variants for now.
- WAN-FLF2V hidden probe is implemented as a separate route, not a T2V entrypoint overload: cache script `experiments/probe3d/scripts/prepare_scrream_wan_flf2v_feature_cache.py`, Slurm scripts `slurm/scrream_wan_flf2v_download.sbatch`, `slurm/scrream_wan_flf2v_precompute.sbatch`, and `slurm/scrream_wan_flf2v_train.sbatch`. It uses `Wan-AI/Wan2.1-FLF2V-14B-720P-diffusers`, `WanImageToVideoPipeline`, `pair_endpoint81` windows (`slot0=f0`, `slot80=f1`, intermediate slots linearly sampled), hidden `t249/layer14`, empty prompt, and `wan_cross_attn_resampler`. Cache root is `experiments/probe3d/feature_cache/scrream_wan_flf2v14b_pair_endpoint81_480`; 480P pair cache shape is `[3120,5120]`. Download/preflight retry `86500`, feature-cache shard jobs `86515` / `86516` / `86517` / `86518`, smoke train `86519`, and formal train `86520` completed. Formal FLF2V result is `F@0.10=0.42897984457682026`, `pred_to_gt_p90=0.5051772321263949`, and `Chamfer=0.14052311765650907`, so do not expand FLF2V hidden broadly yet.
- `train_wan_t2v_nova_adapter.py` exposes `--adapter_gated` for zero-initialized residual gates in WAN CA blocks, and `slurm/scrream_wan_t2v_ablation_pack_train.sbatch` exposes `WAN_ADAPTER_GATED` plus `WAN_SEED`. Static checks and local dummy forward/backward passed on `2026-05-19`.
- WAN is paused as a main branch after the 2026-05-19 audit; final summary is `docs/probe/wan_summary_2026-05-19.md`. Hidden CA stability/capacity jobs `86547` / `86548` / `86549` / `86550` / `86553` / `86554` completed `0:0` on `air-node-02`. L4 `t249/layer14` is a small repeated-F improvement (`F@0.10=0.4919946248489035`) over full-grid `t249/layer14`, but it does not beat the historical `t499/layer14` CA peak (`0.5012956284974035`); gated CA and latent branches should not be expanded. Future WAN work should be appendix-only unless explicitly requested.
- The next active model-coverage step is to choose a new frozen backbone and reuse the clean SCRREAM / NOVA protocol. Start with cache shape / metadata sanity, zero or sample-shuffle control, one-step adapter smoke, and a small layer/readout pilot before any broad ablation.
- Formal SCRREAM and WAN ablation Slurm defaults now use 50 epochs unless explicitly overridden; completed clean-GT VGGT job `86286` was submitted before that change and remains a 30-epoch-equivalent `9510`-step comparison.

Preserve user changes. The worktree may be dirty with unrelated edits and intentionally deleted retired-automation traces.
