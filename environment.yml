name: nova3r
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - git
  - ninja
  - cmake
  - pkg-config
  - setuptools<70
  - wheel
  - pytorch
  - torchvision
  - pytorch-cuda=12.1
  - pip:
      - -r requirements.txt
