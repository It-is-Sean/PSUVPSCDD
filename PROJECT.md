# PROJECT.md

## Current canonical state — 2026-05-19

The active branch is a server-side research workspace for the PSUVPSC3DD probe. The ScanNet experiments remain the diagnostic baseline, but the immediate training branch has moved to **full SCRREAM mesh-complete adapter training** now that the full SCRREAM tree is available at `~/datasets/SCRREAM`.

The current research interpretation is:

- old ScanNet experiments with `max_interval=30` are confounded because processed data already used `frame_skip=20`; use `scannet_max_interval=1` for the intended NOVA3R-style spacing;
- the raw ScanNet GT audit did not show obviously catastrophic data quality;
- oracle/CD rankings were unstable and should not drive claims;
- the interval-corrected MLP baseline has coverage but poor precision/outlier control, so valid progress must improve robust visual-first metrics, not just symmetric CD;
- the first lightweight cross-attention candidate (`p7_k2_i1_anchor_ca_l2_h512_chamfer_step1000`) completed 1000 steps with validation CD `0.54222615`, so it is not an immediate scalar improvement over MLP;
- the old local `eval_scrream` runs remain invalid for claims, but the corrected full SCRREAM branch is now available and should be evaluated with mesh-complete supervision;
- the current SCRREAM target construction is: current sequence `meta.txt` object set -> registered scene meshes -> surface-area-proportional surface sampling -> two-input-view union-frustum crop -> first input camera coordinates -> FPS / pad to fixed target points;
- Slurm is the expected execution path for data generation and training scripts on this machine;
- SCRREAM job `86140` completed the 20k / 500k trainplus-test MLP baseline on `air-node-02` with exit `0:0`, elapsed `00:26:26`, and Slurm end time `2026-05-07 22:40:22 CST`;
- SCRREAM robust VGGT layer-ablation job `86149` completed on `2026-05-08`, but it used the pre-meta-filter GT and is now historical rather than claim-level for the clean SCRREAM target;
- sequence-meta-filtered 20k / 500k SCRREAM adapter data was regenerated by Slurm jobs `86283` and `86284`; clean-GT VGGT layer-ablation job `86286` completed on `air-node-04` with exit `0:0`, elapsed `02:23:11`, under `*robustval_metafilter_seed17_vggt_layerXX`;
- clean-GT VGGT1 MLP layer `16` at 50 epochs is the current default (`best_val_fscore_tau_0.10=0.702544731989172`, `best_val_pred_to_gt_p90=0.19675995161135992`, `best_val_chamfer_l2=0.027118226668486994`); VGGT1 CA layer `20` at 50 epochs is the closest readout comparison (`F@0.10=0.7014525243138799`, `pred_to_gt_p90=0.21181315431992212`, `Chamfer=0.027010128212471802`); old clean-GT VGGT job `86286` layer16 (`F@0.10=0.6860468604251301`) is now a 9510-step historical baseline;
- the SCRREAM 20k baseline output is `experiments/probe3d/result/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_mlp_l4_nova_flow_seed17`; `final_metrics.json` reports `first_loss=1.4599288702011108`, `final_loss=0.8398033976554871`, `best_loss=0.5998285412788391`, and `best_val_chamfer_l2=0.5149603486061096`;
- WAN Route2 is now a paused model-coverage branch after the `2026-05-19` audit. It used the exact same SCRREAM `.pt`, split, mesh-complete GT, MLP adapter, NOVA decoder, and robust validation as the VGGT ablation, but with cached WAN2.1 T2V 81-frame video-context features at timesteps `249,499,749` and code layers `9,14,19,24,29`;
- WAN Route2 uses `third_party/VidFM3D` as a reference submodule, `experiments/probe3d/scripts/prepare_scrream_wan_t2v_feature_cache.py` for cache generation, `experiments/probe3d/train_wan_t2v_nova_adapter.py` for training, and `slurm/scrream_wan_t2v_*.sbatch` for all Slurm runs;
- WAN checkpoint download/preflight completed; the local checkpoint is `checkpoints/wan2.1/Wan2.1-T2V-1.3B-Diffusers` (~27 GB), 2-sample WAN feature smoke job `86282` completed for timestep `749`, layer `20`, and full feature precompute jobs `86292` / `86293` / `86294` completed for the official 15-grid cache;
- WAN cache root `experiments/probe3d/feature_cache/scrream_wan_t2v1p3b_ctx81` contains `4937` `.pt` files / about `45G`; formal training pack job `86307` completed all 15 runs on `air-node-04`, exit `0:0`, elapsed `13:36:04`;
- WAN sanity job `86316` confirmed the cache path is non-degenerate; best formal WAN is `t499/layer09` (`F@0.10=0.46988987902779306`, `pred_to_gt_p90=0.48718947172164917`, `Chamfer=0.21040735269586244`), still far weaker than clean-GT VGGT layer `16`, so this remains a setting-sensitive exploratory branch rather than a claim-level result;
- WAN Route2.1 clean / low-noise audit completed: cache smokes `86342` / `86343`, full caches `86350` / `86351`, smoke train `86357`, and replacement 1-GPU formal packs `86371` / `86372` all completed successfully for layers `9,14,29`;
- Route2.1 did not beat old WAN Route2 best: best clean/low-noise result is `no_noise/layer29` with `F@0.10=0.44840173` and `pred_to_gt_p90=0.48779308`, below old `t499/layer09` `F@0.10=0.46988987902779306`;
- `train_wan_t2v_nova_adapter.py` supports `--wan_feature_norm none|token_layernorm|sample_standardize|token_l2`, and `slurm/scrream_wan_t2v_ablation_pack_train.sbatch` passes it via `WAN_FEATURE_NORM`; targeted `t499/layer09 + token_layernorm` job `86395` completed with `F@0.10=0.45476213575933216`, below old Route2 best;
- `pair_tiled81` versus `ctx81` completed as jobs `86396 -> 86397 -> 86400 -> 86401`; it did not beat the old Route2 best (`F@0.10=0.4429200036613287`, `pred_to_gt_p90=0.5104739194115003`), so simple feature scaling and real-context dilution are not enough to explain the WAN gap;
- WAN predicted-x0 / denoised-latent probing is implemented: cache generation accepts `--feature_kind pred_x0_latent`, training accepts `--wan_feature_kind pred_x0_latent`, cache layout is `t499/pred_x0_latent/<sample>.pt`, and full cache job `86411` completed `329/329` samples with feature shape `[12480,16]`;
- original MLP-L4 pred-x0 readout did not produce a formal result: `86412` failed on the first backward with a PyTorch CUDA adaptive-pooling shared-memory assert, caused by pooling the large `[12480,128]` token sequence to `[768,128]`;
- `WanPredX0LatentConvAdapter` / `--adapter_type conv2d_mlp` is implemented for pred-x0: it restores `[B,2,60,104,16]`, applies a small stride-2 Conv2d readout to `[B,3120,128]`, then pools to NOVA scene tokens; smoke job `86421` and formal job `86422` completed, but pred-x0 conv did not beat old WAN hidden (`F@0.10=0.41336424426176693`, `pred_to_gt_p90=0.6272750149170557`);
- `grid2d_pool` and `grid2d_conv` hidden readouts were added to preserve the WAN hidden `[2,30,52]` grid before mapping to NOVA `[2,24,16]` scene tokens; formal jobs `86424` and `86426` completed with `F@0.10=0.43326753863230877` and `0.39855373112050535`, so simple fixed 2D pooling/conv is not the missing WAN interface;
- `WanHiddenCrossAttentionResamplerAdapter` / `--adapter_type wan_cross_attn_resampler` is the only clearly positive WAN readout: layer sweep job `86429` produced the historical best `t499/layer14` at `F@0.10=0.5012956284974035`, `pred_to_gt_p90=0.4229188362757365`, `Chamfer=0.10908368105689685`; user visual inspection confirmed improvement;
- CA-resampler stability checks completed: the low-LR rerun `86453` for `t499/layer14` was negative (`F@0.10=0.4292316005720653`), and full-grid job `86468` completed all 15 settings with repeated best F-score at `t249/layer14` (`0.48913902331806663`) and best p90 / Chamfer at `t249/layer09` (`0.410525918006897` / `0.11981592203179996`);
- multi-source CA-resampler fusion is implemented: `--adapter_type wan_cross_attn_multilayer_resampler` uses `--wan_layers` to concatenate same-timestep layers, while `--adapter_type wan_cross_attn_multitime_resampler` uses `--wan_timesteps` to concatenate same-layer timesteps; both keep hidden cache tokens and output NOVA scene tokens through the learned resampler;
- multi-layer fusion completed negative versus the strongest single-layer CA baselines: `t249 layers 9+14` job `86485` reached `F@0.10=0.46487238144059545`, `pred_to_gt_p90=0.49916083614031476`, `Chamfer=0.12842474008599916`; `t249 layers 9+14+19` job `86487` wrote complete metrics despite a post-write SwanLab finish failure, reaching `F@0.10=0.4724058254433277`, `pred_to_gt_p90=0.449202927450339`, `Chamfer=0.12805132629970709`;
- multi-timestep same-layer fusion completed negative versus the strongest single-layer CA baselines: smoke job `86493` completed for timesteps `249+499+749`, layer `14`, and formal job `86494` completed with `F@0.10=0.45213117002164466`, `pred_to_gt_p90=0.49119475732247037`, `Chamfer=0.12659800921877226`;
- WAN-FLF2V hidden probing is implemented as a separate branch using `WanImageToVideoPipeline`, not by overloading the T2V feature entrypoint. It uses model `Wan-AI/Wan2.1-FLF2V-14B-720P-diffusers`, cache script `experiments/probe3d/scripts/prepare_scrream_wan_flf2v_feature_cache.py`, and Slurm scripts `slurm/scrream_wan_flf2v_download.sbatch`, `slurm/scrream_wan_flf2v_precompute.sbatch`, and `slurm/scrream_wan_flf2v_train.sbatch`;
- FLF2V window mode is `pair_endpoint81`, not T2V `ctx81`: slot `0` is pair frame `f0`, slot `80` is pair frame `f1`, slots `1..79` linearly sample sequence frame ids between them, and pair features use hidden temporal indices `[0,20]`. The first formal target is `t249/layer14 + wan_cross_attn_resampler`, empty prompt, same SCRREAM `.pt`, same NOVA/FM decoder/loss/validation, and expected 480P cache shape `[3120,5120]`;
- FLF2V checkpoint download/preflight completed as retry job `86500` after initial job `86497` hit a transient HuggingFace incomplete-read error; local checkpoint is under `checkpoints/wan2.1/Wan2.1-FLF2V-14B-720P-diffusers` and is about `84G`. Local py_compile, bash syntax, CA-resampler `5120` dummy shape/backward, and 329-sample window-only smoke passed. Feature cache shard jobs `86515` / `86516` / `86517` / `86518` completed `329/329` samples with shape `[3120,5120]`; smoke train `86519` and formal train `86520` completed. Formal FLF2V result is `F@0.10=0.42897984457682026`, `pred_to_gt_p90=0.5051772321263949`, `Chamfer=0.14052311765650907`, below T2V CA baselines;
- `prepare_scrream_wan_t2v_feature_cache.py` and `train_wan_t2v_nova_adapter.py` support latent tensor-choice probes (`pred_x0_latent` and `model_output_latent`, shape `[12480,16]`), but Conv2d and learned latent-CA readouts completed negative (`86536`, `86540`, `86543`), so stop the latent tensor-choice branch for now;
- `train_wan_t2v_nova_adapter.py` and `slurm/scrream_wan_t2v_ablation_pack_train.sbatch` expose `--adapter_gated` / `WAN_ADAPTER_GATED` plus `WAN_SEED`; hidden CA stability/capacity jobs `86547` / `86548` / `86549` / `86550` / `86553` / `86554` completed. L4 CA `t249/layer14` is the best recent repeated-F result (`F@0.10=0.4919946248489035`), but it does not beat the historical `t499/layer14` CA peak; gated CA and `t499` repeats did not justify expansion;
- the final WAN summary is `docs/probe/wan_summary_2026-05-19.md`; future work should treat WAN follow-ups as optional appendix checks while the main line moves to the next backbone;
- VGGT-Omega is wired as the next-backbone probe. Source is `third_party/vggt-omega`; training uses `experiments/probe3d/train_vggt_nova_adapter.py --backbone vggt_omega`, with launcher `slurm/scrream_vggt_omega_layer_ablation_pack_train.sbatch`. Official weights are `checkpoints/vggt_omega/vggt_omega_1b_512.pt`; `checkpoints/vggt_omega/model.pt` was verified on `2026-05-19` to be byte-identical to old `checkpoints/vggt/model.pt` and must not be used for Omega conclusions. Dense/full-token MLP, dense/full-token CA, and register-only / "Frozen Scene Tokens" CA have completed for layers `12,16,20,24`. Best Omega result is dense CA layer16 (`F@0.10=0.6576072630447046`, `p90=0.23843258867661157`, `Chamfer=0.033605430430422224`), still below VGGT1 MLP layer16 (`0.702544731989172`) and VGGT1 CA layer20 (`0.7014525243138799`);
- VGGT1 50-epoch baseline jobs completed on `2026-05-19`: job `86571` ran MLP-L4-H1024 on layers `12,16,20,24` and completed `0:0` in `02:08:56`; job `86572` ran cross_attention-L4-H1024 on the same layers and completed `0:0` in `02:13:51`. MLP layer16 is the new default; CA layer20 is the key readout comparison because it nearly ties MLP layer16 and dramatically improves over MLP layer20;
- default formal SCRREAM / WAN ablation runs should now use `50` epochs unless an experiment explicitly overrides the epoch count;
- local checkpoints now include `checkpoints/scene_n1/checkpoint-last.pth`, `checkpoints/scene_n2/checkpoint-last.pth`, `checkpoints/scene_ae/checkpoint-last.pth`, and `checkpoints/vggt/model.pt`.

The immediate handoff contract is described in `docs/probe/handoff_2026-05-07.md`, `experiments/probe3d/README.md`, and `docs/probe/experiment_plan.md`.

## Project identity

This repo is currently a **research execution workspace** for probing whether strong frozen visual features can be adapted into a NOVA3R-style scene reconstruction decoder with minimal trainable structure.

The present focus is intentionally narrow:

- image / geometry first
- reliable supervision first
- scale validation before method sprawl

## Current status

There are now three branches, with different evidential status.

### A. SCRREAM full-data branch

The earlier local `eval_scrream` experiments were later found to use only the released **evaluation subset**, not the official full SCRREAM training-scale dataset.

Therefore:

- those old SCRREAM numbers are **invalid as formal evidence**
- they remain useful as engineering/debug history only
- the corrected SCRREAM rerun must use the full tree at `~/datasets/SCRREAM`

Current implementation status:

- data bridge: `experiments/probe3d/scripts/prepare_scrream_full_adapter_data.py`
- primary target source: `--target_source mesh_complete`
- mesh source: current sequence `meta.txt` object names matched against `sceneXX/meshes/*.obj`
- pair source: `data/scrream/scrream_n2_list.json`
- target coordinates: first input camera frame
- generated target shapes: `[329, 10000, 3]` and `[329, 20000, 3]`
- loader path: `python experiments/probe3d/train_vggt_nova_adapter.py --dataset scrream_adapter --data_root <adapter.pt>`
- Slurm prep script: `slurm/scrream_mesh_complete_prepare.sbatch`
- Slurm split-merge helper: `slurm/scrream_merge_test_into_train.sbatch`
- Slurm MLP training script: `slurm/scrream_mesh_complete_mlp_train.sbatch`
- current formal adapter input:
  - `experiments/probe3d/adapter_data/scrream_mesh_complete_n2_adapter_seed17_tp20000_ms500000_trainplus_test.pt`
  - shape `[329, 20000, 3]`, split `train=317`, `val=12`, `mesh_sequence_meta_filter=True`
- old pre-meta-filter `.pt` files were moved to `experiments/probe3d/adapter_data/deprecated_meta_filter_bug/`
- completed Slurm training job recorded on 2026-05-07:
  - `86140` / `scrream_mesh_mlp`: `COMPLETED`, exit `0:0`, elapsed `00:26:26`, node `air-node-02`
  - output: `experiments/probe3d/result/scrream_mesh_complete_n2_trainplus_test_tp20000_ms500000_mlp_l4_nova_flow_seed17`
  - best validation Chamfer-L2: `0.5149603486061096`
- network proxy default in the Slurm scripts: `http://127.0.0.1:7896`
- WAN repo/checkpoint Slurm jobs use `http://127.0.0.1:17890` and create an SSH tunnel from compute nodes to `air-server:127.0.0.1:17890` by default
- SwanLab is installed in `nova3r`, and the full MLP script enables SwanLab by default unless `SCRREAM_SWANLAB=0`
- `slurm/scrream_mesh_complete_mlp_train.sbatch` supports single-node DDP with `SCRREAM_GPUS_PER_NODE`; prefer `SCRREAM_EPOCHS` for future multi-GPU runs so step counts scale by effective batch size.
- Local `scrream_official_depth_mix_*` artifacts are historical ablations and not part of the current training line.

### B. ScanNet v2 branch

The ScanNet v2 mesh-first extension is now the diagnostic baseline.

It should be interpreted as:

- a **NOVA3R-style extension / transfer probe** on ScanNet v2
- not a literal reproduction of official NOVA3R training on `3D-FRONT + ScanNet++V2`

Still, this line is now serious enough to serve as the proposal’s **reliable-target baseline**.

### C. WAN2.1 T2V Route2 branch

The WAN branch is the immediate representation-comparison follow-up to the completed clean-GT VGGT rerun. The old completed VGGT layer ablation remains useful history, but it used pre-meta-filter GT.

It intentionally holds the training problem fixed:

- adapter data: `experiments/probe3d/adapter_data/scrream_mesh_complete_n2_adapter_seed17_tp20000_ms500000_trainplus_test.pt`
- split: `train=317`, `val=12`
- GT: `target_source=scrream_registered_mesh_complete`, `target_points=20000`, `mesh_sample_points=500000`
- adapter and loss: `MLP-L4-H1024`, `nova_flow`, `num_queries=20000`
- validation: full val split, robust one-way/F-score/trimmed-CD metrics, `val_visual_40960/`

Only the representation changes. WAN T2V is loaded during feature precompute, not during every adapter-training step. Cached features are stored per `(timestep, layer, sample)` with shape `[3120,1536]`.

Current code and Slurm entrypoints:

- `experiments/probe3d/requirements-wan-t2v.txt`
- `experiments/probe3d/scripts/prepare_scrream_wan_t2v_feature_cache.py`
- `experiments/probe3d/train_wan_t2v_nova_adapter.py`
- `slurm/scrream_wan_t2v_download.sbatch`
- `slurm/scrream_wan_t2v_precompute.sbatch`
- `slurm/scrream_wan_t2v_ablation_pack_train.sbatch`
- `slurm/scrream_wan_t2v_multilayer_pack_train.sbatch`
- `slurm/scrream_wan_t2v_multitime_pack_train.sbatch`

The full 15-grid feature cache has completed, and the full 15-run WAN training pack completed as Slurm job `86307`. The first attempt `86306` failed before training due a scalar bookkeeping bug, not a data/model failure; the failed output directory was renamed with `_failed_86306`.

Current interpretation: the WAN path is paused after producing a useful negative result. T2V hidden states become materially stronger when read through a learned CA resampler. The old MLP hidden baseline `t499/layer09` was `F@0.10=0.46988987902779306`; CA resampler has a historical peak at `t499/layer14` (`0.5012956284974035`) and a more repeatable full-grid signal around `t249/layer09/14` (`0.4766100401399189` / `0.48913902331806663`). Simple same-timestep multi-layer fusion, same-layer multi-timestep fusion, FLF2V first/last-frame hidden, latent tensor-choice probes, and gated residual CA did not improve over those single-layer hidden CA baselines. WAN is still well below clean-GT VGGT layer `16`, and CA-resampler training variance is a first-order issue, but the interface/readout lesson is real. The main line now moves to the next backbone; see `docs/probe/wan_summary_2026-05-19.md` for the frozen WAN conclusion.

### D. InteriorGS branch

InteriorGS remains a deferred high-quality data option. Do not treat it as the immediate next training branch until the completed SCRREAM mesh-complete baseline has been inspected and understood.

## Formal ScanNet v2 setup

### Data / supervision
- mesh source: `vh_clean.ply`
- frame extraction: `frame_skip=20`
- GT definition: `mesh surface ∩ sparse-input-view union frustum`
- no extra visible/occluded auxiliary labels in this version
- scene-level complete reservoir: `500k` points / scene

### Processed dataset
- processed root:
  - `/data1/jcd_data/scannet_processed_large_f20_vhclean500k`
- split root:
  - `/data1/jcd_data/scannet_processed_large_f20_vhclean500k_split_seed17`
- scene counts:
  - train = `1362`
  - val = `151`
  - test = `100`

### Training stack
- true 4-view input
- `pts3d_complete` available in dataset batches
- MLP / CA / SA launchers converted to `torchrun` / DDP

## Current probe baseline

The most informative current run is the short ScanNet probe, not the old long formal MLP schedule.

### Best numeric configuration
- target mode: `anchor_frustum`
- adapter: `MLP-L4, hidden=1024`
- decoder: frozen NOVA-style scene decoder
- training objective: direct sampled rollout Chamfer (`loss_type=chamfer_sample`)
- training schedule:
  - `lr=5e-5` to step 2000
  - resume with `lr=1e-5` to step 2500
- best validation CD: `0.08745259`
- output dir:
  - `experiments/probe3d/result/probe_trials/p1_adapter_anchor_frustum_mlp_l4_chamfer_lr1e5_refine_step2500`

### Interpretation
- `nova_flow` objective was mismatched with the final sampled-point Chamfer evaluation.
- Direct rollout Chamfer pushed validation CD below `0.1`.
- Visualized point clouds remain visibly noisy / thick / outlier-heavy.
- Current bottleneck is therefore not just CD minimization, but prediction precision and distribution sharpness.

## What has already been validated

- ScanNet visible-depth smoke passed
- ScanNet complete-GT smoke passed
- full-root `torchrun --nproc_per_node=1` MLP DDP preflight passed
- mesh / pose alignment looks valid on sampled scenes
- full preprocess completed successfully

## Immediate next step

1. use clean-GT VGGT1 MLP layer `16` 50ep as the current default and VGGT1 CA layer `20` 50ep as the close readout comparison
2. use `docs/probe/wan_summary_2026-05-19.md` as the final WAN evidence trail and do not launch new broad WAN sweeps
3. treat the negative Omega-vs-VGGT1 result as a project-level validity warning, not just another layer-choice issue
4. implement a decoder-free SCRREAM spatial readout on the same frozen VGGT1/Omega features to test whether Omega is actually better outside the NOVA/FM generator interface
5. if decoder-free Omega beats VGGT1 but NOVA/FM Omega loses, reframe this project line as measuring compatibility with the fixed generator condition-token manifold rather than pure spatial representation quality
6. keep optional WAN follow-ups limited to appendix checks, such as L4 `t249/layer09` or an L4 `t249/layer14` seed repeat
7. expand SCRREAM training sample scale beyond 329 official pairs only after the probe-validity audit is understood
8. keep InteriorGS as a data-quality option only after SCRREAM scale/model coverage is better understood

## Supporting docs

- `docs/probe/handoff_2026-05-07.md`
- `docs/probe/scannet_mesh_first_plan.md`
- `docs/probe/experiment_history.md`
- `docs/probe/experiment_plan.md`
- `docs/probe/interiorgs_training_plan.md`
- `docs/probe/todo.md`
- `experiments/probe3d/README.md`

### Paper-aligned NOVA3R reset

After user review, the active plan is to align the ScanNet target/loss more closely with NOVA3R: complete / amodal points inside the selected input-view frustum, FPS-style target sampling through `src_complete_fps_*`, and native flow matching as the primary loss. The new phase-2 config is:

- `experiments/probe3d/probe_trials/configs/phase2_nova_aligned.json`
